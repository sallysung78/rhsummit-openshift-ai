# 데모 — OpenShift AI에서의 모델 관리

이 데모의 요점은 클러스터가 LLM을 돌릴 수 있다는 것이 아닙니다. **같은 모델이 한 곳에서
추적되면서 스토리지 → 레지스트리 → GPU 엔드포인트로, 그리고 되돌아 오는 것**, 그리고
버전 교체가 누군가 기억에 의존해 YAML을 고치는 일이 아니라 의도적이고 눈에 보이는
행위가 된다는 것입니다.

## 이미 갖춰진 것

2026-09-20 클러스터에서 확인 — 아래는 새로 만들 필요가 없습니다:

| 구성요소 | 상태 |
|---|---|
| Model Registry | `default-modelregistry` **Ready**, `rhoai-model-registries`의 Postgres 기반 |
| 모델 카탈로그 UI | https://model-catalog.apps.<CLUSTER>.<BASE_DOMAIN> |
| vLLM GPU 런타임 | `vllm-cuda-runtime-template` 존재 |
| KServe | `Managed`, RawDeployment 모드 |
| GPU | 1 × L40S 48 GB, `nvidia.com/gpu` allocatable 1, HardwareProfile `l40s-gpu` |
| Pipelines | `aipipelines` Managed — 프로젝트별 서버는 별도 구성 필요 |
| **오브젝트 스토리지** | **`s3://<INFRA_ID>-models`, 이 데모를 위해 생성** |

## 오브젝트 스토리지

쓸 만한 오브젝트 스토리지가 없었습니다. 계정에 있던 유일한 버킷
`<INFRA_ID>-image-registry-us-east-2-…`는 OpenShift 내부 컨테이너 레지스트리 소유이며,
여기에 모델 아티팩트를 쓰면 레지스트리 오퍼레이터가 정리해 버리거나 더 나쁜 일이
생길 수 있습니다.

대신 새로 만들었습니다:

| | |
|---|---|
| 버킷 | `<INFRA_ID>-models` (us-east-2) |
| 프리픽스 | `models/`, `pipelines/` |
| 보안 | 퍼블릭 액세스 차단, SSE-S3 암호화 |
| 자격증명 | IAM 사용자 `<INFRA_ID>-rhoai-s3`, 키는 `s3-creds.env` |

이 키는 의도적으로 admin 키가 **아닙니다.** 인라인 정책이 이 버킷 하나에 대해서만
`s3:GetObject/PutObject/DeleteObject/ListBucket`을 허용합니다 — image-registry 버킷,
IAM, EC2에서 실제로 거부되는 것을 확인했습니다.

## 모델

L40S는 가격이 시사하는 것보다 좋은 카드입니다. VRAM 48 GB면 4비트 양자화된 320억
파라미터 모델이 들어가고 긴 컨텍스트를 위한 여유도 남습니다. Hugging Face API에서 읽은
실제 가중치 크기:

| 모델 | 가중치 | 45 GiB 카드 기준 |
|---|---|---|
| [Qwen3-8B-AWQ](https://huggingface.co/Qwen/Qwen3-8B-AWQ) | 5.7 GiB | 여유로움 |
| [Qwen3-14B-AWQ](https://huggingface.co/Qwen/Qwen3-14B-AWQ) | 9.3 GiB | 넉넉함 |
| **[Qwen3-32B-AWQ](https://huggingface.co/Qwen/Qwen3-32B-AWQ)** | **18.0 GiB** | **KV 캐시로 약 26 GiB 남음** |
| [Qwen3-32B-FP8](https://huggingface.co/Qwen/Qwen3-32B-FP8) | 32.0 GiB | 빠듯함 |
| [Qwen3-30B-A3B](https://huggingface.co/Qwen/Qwen3-30B-A3B) | 56.9 GiB | 안 들어감 (BF16) |

**8B와 32B를 둘 다 쓰세요.** `Qwen3-8B-AWQ`를 v1으로, `Qwen3-32B-AWQ`를 v2로 두는 것은
타협이 아닙니다 — 둘 사이의 업그레이드가 곧 데모입니다. 등록할 실제 버전 변경,
수행할 실제 재배포, GPU 대시보드에서 보이는 변화가 전부 플랫폼 안에서 나옵니다.

## 시나리오

### 0. Data Science Project와 연결

RHOAI 대시보드에서 프로젝트(`rhsummit`)를 만들고 `s3-creds.env`의 값으로 S3 연결을
추가합니다. 자격증명을 타이핑하는 곳은 여기 한 곳뿐이고, 이후 모든 단계는 연결을
이름으로 참조합니다.

### 1. 가중치를 S3로 — 노트북이 아니라 클러스터에서

18 GiB를 가정용 회선으로 받는 것은 방향이 틀렸습니다. 클러스터 안에서 돌리면
Hugging Face에서 받는 것도 S3로 올리는 것도 AWS 네트워크에서 일어납니다:

```yaml
apiVersion: batch/v1
kind: Job
metadata:
  name: fetch-qwen3-32b
  namespace: rhsummit
spec:
  backoffLimit: 1
  template:
    spec:
      restartPolicy: Never
      containers:
      - name: fetch
        image: registry.access.redhat.com/ubi9/python-311
        envFrom: [{ secretRef: { name: s3-models } }]
        command: ["bash","-lc"]
        args:
          - |
            set -euo pipefail
            pip install -q --no-cache-dir huggingface_hub awscli
            hf download Qwen/Qwen3-32B-AWQ --local-dir /scratch/m >/dev/null
            aws s3 sync /scratch/m "s3://$AWS_S3_BUCKET/models/qwen3-32b-awq/" --only-show-errors
        volumeMounts: [{ name: scratch, mountPath: /scratch }]
        resources:
          # emptyDir은 ephemeral-storage 한도에 산입됩니다. 가중치 18 GiB를
          # 넉넉히 넘겨야 합니다 — 4Gi로 잡았다가 파드가 Evicted 됐습니다.
          requests: { cpu: "2", memory: 6Gi, ephemeral-storage: 30Gi }
          limits:   { cpu: "4", memory: 12Gi, ephemeral-storage: 50Gi }
      volumes:
      - name: scratch
        emptyDir: { sizeLimit: 50Gi }
```

**말할 거리:** 이제 모델은 남의 URL이 아니라 우리 스토리지 안의 아티팩트입니다.
이후 과정이 감사 가능해지는 이유가 그것입니다.

### 2. Model Registry에 v1 등록

그 S3 URI를 가리키는 `qwen3-8b-awq`를 등록하고, 누가 이 모델을 써도 되는지 판단하게
해주는 메타데이터를 채웁니다: 출처, 라이선스, 양자화(`AWQ 4-bit`), 용도, 등록자.

**말할 거리:** 레지스트리는 `.safetensors`가 담긴 폴더가 답하지 못하는 질문에 답합니다 —
이건 어디서 왔는지, 무엇을 해도 되는지, 어느 버전이 프로덕션에 있는지, 누가 승인했는지.

### 3. GPU에 배포

레지스트리 항목에서 `vllm-cuda-runtime` 런타임과 **`l40s-gpu`** 하드웨어 프로파일로
KServe `InferenceService`를 배포합니다. 프로파일이 중요합니다: GPU 노드에
`nvidia.com/gpu=true:NoSchedule` 테인트가 걸려 있고, 대응하는 톨러레이션을 들고 있는 것이
바로 이 프로파일입니다 — 없으면 파드가 Pending에 머뭅니다.

8B용 vLLM 인자 예시:

```
--max-model-len=16384
--gpu-memory-utilization=0.90
--quantization=awq_marlin
```

### 4. 실제로 서빙되는지 확인

KServe는 OpenAI 호환 API를 노출합니다:

```bash
curl -sk https://<inference-endpoint>/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen3-8b-awq","messages":[{"role":"user","content":"자기소개를 두 문장으로 해줘"}]}'
```

### 5. v2 등록 — 이것이 진짜 요점

1단계를 `Qwen/Qwen3-32B-AWQ`로 반복해 `models/qwen3-32b-awq/`에 넣고, **같은 등록 모델의
새 버전**으로 등록한 뒤 재배포합니다.

`--max-model-len`을 올리고 `--gpu-memory-utilization=0.90`은 유지하세요. 가중치 18 GiB면
캐시에 쓸 공간이 넉넉합니다.

**말할 거리:** 항목 하나, 버전 둘, 변경 사유 기록, 그리고 발굴 작업이 아니라 v1 재배포로
끝나는 롤백.

### 6. 비용과 용량 이야기

OpenShift 콘솔의 **Observe → Dashboards → NVIDIA GPU / DCGM** 으로 전환해 프레임버퍼가
약 6 GiB에서 18 GiB로 뛰는 것과 생성 중 텐서 파이프 활성도를 보여줍니다.

그다음 `GPUNodeUnallocated` 알림을 보여주세요 — GPU가 요청 없이 한 시간 놀면 발동합니다.
아무도 쓰지 않는 시간당 $2.24짜리 카드야말로 이 환경이 드러내도록 설계된 실패 모드입니다.

## 선택 사항: 파이프라인

데모에 자동화까지 넣으려면 프로젝트에서 파이프라인 서버를 구성하되
`s3://<INFRA_ID>-models`에 basePath `pipelines/`를 지정하세요 — 버킷에 해당 프리픽스가
이미 있습니다. fetch → register → deploy를 수행하는 파이프라인이 1~3단계를 클릭 한 번으로
바꿔줍니다.

선택 사항이고 움직이는 부품이 늘어납니다. 레지스트리 이야기를 먼저 완성하세요.

## 리허설용 소요 시간

| 단계 | 예상 |
|---|---|
| 8B를 S3로 | 수 분 (실측: HF 다운로드 30초 + S3 동기화) |
| 32B를 S3로 | 약 10~15분 |
| 첫 vLLM 기동 (이미지 풀 + 로드) | 5~10분 |
| 이후 기동 | 2~3분 |

**데모 전에 두 모델을 미리 S3에 넣어 두세요.** 18 GiB 다운로드를 구경하고 싶은 사람은
없습니다.

---

이전: [3단계 — GPU 노드 연결](03-gpu-노드-연결.md) · 다음: [5단계 — 촬영 대본](05-촬영-대본.md)
