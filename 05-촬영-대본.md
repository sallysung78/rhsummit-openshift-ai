# 촬영 대본 — RHSummit 프로젝트에서 모델 레지스트리 등록과 배포

카메라 앞에서 기다리게 만들 만한 것은 전부 미리 끝내 두었습니다. 남은 것은 찍을 가치가
있는 부분입니다.

## 이미 준비됨 (촬영하지 않아도 되는 것)

| | |
|---|---|
| 프로젝트 | `rhsummit`, 표시명 **RHSummit** — 이미 Data Science Project |
| 버킷 | `s3://<INFRA_ID>-models`, us-east-2, 암호화, 퍼블릭 액세스 차단 |
| 자격증명 | IAM 사용자 `<INFRA_ID>-rhoai-s3`, 이 버킷 전용 |
| 연결 | RHSummit의 **S3 - model storage** — 키 이미 저장됨 |
| S3의 가중치 | `models/qwen3-8b-awq/`, `models/qwen3-32b-awq/` |

연결을 미리 만들어 둔 이유는 **카메라 앞에서 시크릿 키를 타이핑하지 않기 위해서**입니다.

## 이 클러스터에서 확인한 정확한 문자열

| 위치 | 정확한 표기 |
|---|---|
| 연결 유형 | `S3 compatible object storage - v1` |
| 연결 이름 | `S3 - model storage` |
| 서빙 런타임 | `vLLM NVIDIA GPU ServingRuntime for KServe` |
| 모델 프레임워크 / 포맷 | `vLLM` |
| 하드웨어 프로파일 | `NVIDIA L40S (1 x 48GB)` |
| 모델 경로 (v1) | `models/qwen3-8b-awq/` |
| 모델 경로 (v2) | `models/qwen3-32b-awq/` |

**하드웨어 프로파일은 선택 사항이 아닙니다.** GPU 노드에
`nvidia.com/gpu=true:NoSchedule` 테인트가 걸려 있고 대응하는 톨러레이션을 들고 있는 것이
이 프로파일입니다. 다른 것을 고르면 촬영 중에 파드가 `Pending`으로 멈춥니다.

---

## Part 1 — 프로젝트와 모델이 있는 곳 (약 1분)

1. **https://rh-ai.apps.<CLUSTER>.<BASE_DOMAIN>** 을 열고 `kubeadmin`으로 로그인.
2. **Data science projects** → **RHSummit** 으로 이동.
3. **Connections** 탭을 열고 **S3 - model storage** 를 보여줍니다.

   *멘트:* 자격증명은 버킷 하나로 제한된 채 여기 한 번만 저장됩니다. 이후의 레지스트리,
   배포, 파이프라인 어느 것도 키 자체를 보지 않고 이름으로 연결을 참조합니다.

## Part 2 — 모델 등록 (약 2분)

4. **Models** → **Model registry** (레지스트리 `default-modelregistry`).
5. **Register model**:
   - Model name: `qwen3-awq`
   - Version: `v1-8b`
   - Model location: 기존 연결 **S3 - model storage** 선택
   - Path: `models/qwen3-8b-awq/`
   - Model format: `vLLM`
6. 중요한 메타데이터를 채웁니다 — 출처 `Qwen/Qwen3-8B-AWQ`, 라이선스 Apache-2.0,
   양자화 `AWQ 4-bit`, 용도 한 줄 설명.

   *멘트:* 이것이 레지스트리와 폴더의 차이입니다. 폴더는 `.safetensors`를 담을 뿐입니다.
   레지스트리는 이게 어디서 왔고, 무엇을 해도 되고, 어느 버전이 살아있고, 누가 넣었는지에
   답합니다.

## Part 3 — GPU에 배포 (약 3분 + 대기)

7. 모델 버전에서 **Deploy** 선택.
8. 입력:
   - Project: **RHSummit**
   - Serving runtime: **vLLM NVIDIA GPU ServingRuntime for KServe**
   - Model framework: **vLLM**
   - Hardware profile: **NVIDIA L40S (1 x 48GB)**
   - 외부 라우트 / 토큰 인증: 데모라면 외부 라우트 켜고 토큰 인증 끄면 curl이 간단합니다
9. 배포한 뒤 화면을 전환하세요. 첫 기동은 vLLM 이미지를 받고 가중치 5.7 GiB를
   로드하므로 **처음에는 5~10분**, 이후에는 2~3분입니다.

   *기동을 기다리며 할 멘트:* 여기서 손으로 쓴 YAML은 하나도 없습니다. 레지스트리 항목이
   스토리지 위치와 포맷을 들고 있었고, 하드웨어 프로파일이 GPU 요청과 톨러레이션을
   들고 있었습니다.

## Part 4 — 서빙 확인 (약 1분)

10. 배포에서 추론 엔드포인트를 복사한 뒤:

```bash
curl -sk https://<endpoint>/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen3-awq","messages":[{"role":"user","content":"Red Hat OpenShift AI를 한 문장으로 설명해줘"}]}' \
  | python3 -m json.tool
```

    *멘트:* OpenAI 호환 엔드포인트라 기존 클라이언트 코드를 그대로 이쪽으로 돌리면 됩니다.

## Part 5 — 버전 업그레이드, 이것이 진짜 이야기 (약 3분)

11. **Model registry** → `qwen3-awq` → **Register new version**:
    - Version: `v2-32b`
    - Path: `models/qwen3-32b-awq/`
    - 설명에 변경 사유를 적습니다.
12. 같은 방식으로 `v2-32b`를 배포합니다. 같은 GPU, 같은 프로파일.
13. 레지스트리에서 두 버전과 어느 쪽이 배포됐는지를 나란히 보여줍니다.

    *멘트:* 모델 하나, 버전 둘, 기록된 사유, 그리고 v1을 다시 배포하기만 하면 되는 롤백.
    사람들이 "모델 관리"라고 말할 때 실제로 의미하는 것이 이것입니다.

## Part 6 — 비용과 용량 (약 1분)

14. **OpenShift 콘솔** → **Observe** → **Dashboards** → **NVIDIA GPU / DCGM** 으로 전환.
15. 프레임버퍼가 8B에서 약 6 GiB, 32B에서 약 18 GiB로 뛰는 것과 생성 중 텐서 파이프
    활성도를 보여줍니다.
16. **Observe** → **Alerting** 에서 `GPU`를 검색해 `GPUNodeUnallocated`를 가리킵니다.

    *멘트:* 이 카드는 누가 쓰든 안 쓰든 시간당 $2.24입니다. 그래서 플랫폼이 조용히
    청구되게 두지 않고 놀고 있다고 알려줍니다.

---

## 촬영 중 각 단계를 CLI로 확인할 수 있습니다

어느 단계 뒤에든 말씀하시면 UI가 주장한 대로 객체가 실제로 만들어졌는지 확인해 드립니다:

```bash
# 등록 후
oc -n rhsummit get inferenceservice
# 배포 후
oc -n rhsummit get pods -l serving.kserve.io/inferenceservice
oc -n rhsummit describe pod -l serving.kserve.io/inferenceservice | grep -A5 Events
# GPU가 실제로 점유됐는지
oc get node -l node-role.kubernetes.io/gpu -o jsonpath='{.items[0].status.allocatable.nvidia\.com/gpu}{"\n"}'
```

## 솔직한 단서 두 가지

**내비게이션 문구.** RHOAI 3.5 왼쪽 메뉴의 정확한 텍스트 — 레지스트리가 **Models** 하위인지
최상위인지 — 는 확인하지 못했습니다. 읽으려면 대화형 로그인이 필요한데 비밀번호를 입력할
수 없기 때문입니다. 위의 모든 *값*(연결 유형, 런타임, 포맷, 하드웨어 프로파일, 경로)은
클러스터에서 읽은 정확한 값입니다. 메뉴가 여기 적힌 위치에 없더라도 한 단계 이내입니다.

**리허설을 하세요.** 촬영 전에 전체 경로를 한 번 걸어보시되 특히 첫 배포를 해보세요.
그 첫 vLLM 기동이 유일하게 느린 단계이고, 한 번 겪어두면 이미지가 노드에 캐시됩니다.

---

이전: [4단계 — 모델 배포 시나리오](04-모델-배포-시나리오.md) · [처음으로](README.md)
