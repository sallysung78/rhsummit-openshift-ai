# 촬영 대본 — 모델 반입·검증과 버전 전환

카메라 앞에서 기다리게 만들 만한 것은 전부 미리 끝내 두었습니다. 남은 것은 찍을 가치가
있는 부분입니다.

## 이야기의 뼈대

이 데모가 보여주려는 것은 "LLM이 돌아간다"가 아닙니다.

> **Red Hat 검증 모델을 골랐는데 하드웨어가 받아주지 않았다. 그래서 다른 모델로
> 바꿨고, 그 판단의 근거가 레지스트리에 남아 있다. 그리고 같은 판단을 파이프라인이
> 반복해서 내린다.**

모델 관리라는 말이 실제로 무엇을 뜻하는지를 이 한 문장이 설명합니다. 폴더에
`.safetensors` 파일만 쌓아두면 할 수 없는 이야기입니다.

## 이미 준비됨 (촬영하지 않아도 되는 것)

| | |
|---|---|
| 프로젝트 | Data Science Project, S3 연결 등록 완료 |
| 모델 레지스트리 | 인스턴스 생성 완료 (컴포넌트와 별개) |
| S3의 가중치 | 35B FP8(34.9 GiB) · 32B AWQ(18 GiB) · 8B AWQ(5.7 GiB) |
| 레지스트리 항목 | `qwen-demo` — `v1-35b-fp8`(retired) · `v2-32b-awq`(active) |
| 배포 | `qwen3-32b-awq` 기동 완료 |
| 검증 파이프라인 | `model-intake-validation` 업로드 완료, **실행 결과 1건 미리 확보** |
| MLflow | experiment `rhsummit-model-validation` 에 측정 기록 있음 |

연결을 미리 만들어 둔 이유는 **카메라 앞에서 시크릿 키를 타이핑하지 않기 위해서**입니다.

## 필요한 정확한 문자열

| 위치 | 표기 |
|---|---|
| 연결 유형 | `S3 compatible object storage - v1` |
| 서빙 런타임 | `vLLM NVIDIA GPU ServingRuntime for KServe` |
| 모델 프레임워크 / 포맷 | `vLLM` |
| 하드웨어 프로파일 | GPU 프로파일 (예: `NVIDIA L40S (1 x 48GB)`) |
| 파이프라인 이름 | `model-intake-validation` |
| MLflow experiment | `rhsummit-model-validation` |

**하드웨어 프로파일은 선택 사항이 아닙니다.** GPU 노드에 테인트가 걸려 있고 대응하는
톨러레이션을 이 프로파일이 들고 있습니다. 다른 것을 고르면 파드가 `Pending`에 멈춥니다.

---

## Part 1 — 모델은 어디서 오는가 (약 1분)

1. OpenShift AI 대시보드 로그인.
2. **모델 카탈로그**를 엽니다. Red Hat 검증 모델 목록을 보여줍니다.

   *멘트:* 카탈로그는 **읽는 곳**입니다. 남이 만들어 검증한 모델의 후보 목록이고,
   "어떤 모델이 있나"에 답합니다.

3. **Connections** 탭에서 S3 연결을 보여줍니다.

   *멘트:* 자격증명은 버킷 하나로 제한된 채 여기 한 번만 저장됩니다. 이후 레지스트리도
   배포도 파이프라인도 키를 직접 보지 않고 이름으로 참조합니다.

## Part 2 — 검증 모델을 등록한다 (약 2분)

4. **모델 레지스트리**로 이동. `qwen-demo` → `v1-35b-fp8`.
5. 메타데이터를 보여줍니다 — 출처, 양자화 `FP8 W8A8`, 가중치 34.9 GiB,
   `red_hat_validated: true`.

   *멘트:* 레지스트리는 **쓰는 곳**입니다. "우리가 무엇을 쓰고, 누가 넣었고, 어느
   버전이 운영 중인가"에 답합니다. 카탈로그가 서점이라면 레지스트리는 우리 책장과
   대출 기록입니다.

## Part 3 — 하드웨어가 받아주지 않았다 (약 2분) ★ 핵심

6. `v1-35b-fp8`의 상태를 보여줍니다 — **`retired - VRAM 제약`**.
7. 왜 그런지 설명합니다. 실제 오류를 띄워도 좋습니다:

```
torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 1.03 GiB.
GPU has 44.39 GiB total, of which 1001.38 MiB is free.
```

   *멘트:* 가중치 34.9 GiB는 다 올라갔습니다. 그 위에 얹을 KV 캐시와 CUDA 그래프
   자리가 없었습니다. 48 GB 카드라고 48 GB짜리 모델이 도는 게 아닙니다.

   CUDA 그래프를 끄고 컨텍스트를 4096으로 줄이면 기동은 됩니다 — 하지만 느리고
   짧습니다. **"돌긴 한다"와 "쓸 만하다"는 다릅니다.**

   *멘트:* 그 차이를 말로 하지 않고 숫자로 보여드리겠습니다. 다음 장면에서
   파이프라인이 두 모델을 같은 방식으로 재고 판정합니다.

> **실제로 얼마나 다른지는 Part 5에 있습니다** — 출력 처리량 3.7배, 첫 토큰 지연
> 3배입니다. Part 3에서 숫자를 미리 말하지 말고 궁금하게 남겨 두세요.

## Part 4 — 다른 모델로 바꾼다 (약 3분)

8. `v2-32b-awq`를 보여줍니다 — AWQ 4비트, 18 GiB, `active`.
9. 배포 화면에서 런타임과 하드웨어 프로파일을 고릅니다.

   *멘트:* 같은 카드에서 18 GiB 모델은 4분 만에 올라오고, 컨텍스트 16K에 CUDA 그래프도
   켭니다. Red Hat 검증 목록에는 없지만 **우리 하드웨어에는 이쪽이 맞습니다.**
   그 판단이 레지스트리에 기록으로 남습니다.

10. 두 버전을 나란히 보여줍니다.

| 버전 | 양자화 | 크기 | 검증 | 상태 |
|---|---|---|---|---|
| v1-35b-fp8 | FP8 W8A8 | 34.9 GiB | true | retired — VRAM 제약 |
| v2-32b-awq | AWQ 4-bit | 18.0 GiB | false | active |

## Part 5 — 판단을 자동화한다 (약 3분) ★ 두 번째 핵심

지금까지는 사람이 눈으로 보고 판단했습니다. **같은 판단을 파이프라인이 반복해서
내리게** 만들 수 있습니다. 이 데모에서 "모델 관리"라는 말이 가장 구체적으로 보이는
구간입니다.

11. **Data Science Pipelines → Pipelines** → `model-intake-validation` 을 열어
    그래프를 보여줍니다.

    *멘트:* 8단계입니다. GPU가 1장이라 순서가 강제됩니다 — 기준 모델을 내리고,
    후보를 올리고, 한 번 호출해 보고, 성능을 재고, 기준과 비교해 판정하고,
    통과하면 표시를 남기고, **무조건 되돌립니다.**

12. `teardown` 노드를 가리킵니다.

    *멘트:* 이 단계가 없으면 파이프라인이 유일한 GPU를 잡은 채로 끝납니다.
    통과든 기각이든 반드시 실행되도록 붙여 두었습니다. 검증이 운영을 망가뜨리지
    않게 하는 장치입니다.

13. **미리 돌려둔 실행**을 엽니다. `gate` 단계의 로그를 확대합니다.

```
REJECTED: output throughput 9.87 < required 30.00;
          mean TTFT 311.29ms > allowed 200.00ms
```

    *멘트:* 기각입니다. 그래프에서 `promote` 가 회색인 것을 보여주세요 — 실행되지
    않았습니다. 그리고 `teardown` 은 실행됐습니다.

14. 두 모델의 측정값을 나란히 보여줍니다.

| 지표 | Qwen3-32B-AWQ | Qwen3.6-35B-A3B-FP8 |
|---|---|---|
| 출력 토큰/초 | **36.87** | 9.87 |
| 첫 토큰 지연 | **105 ms** | 311 ms |
| 토큰 간 지연 | **26.5 ms** | 99.9 ms |

    *멘트:* Part 3에서 "돌긴 한다와 쓸 만하다는 다르다"고 말한 것의 숫자가 이것입니다.
    35B는 기동됩니다. 그런데 3.7배 느립니다. 이 판정을 사람의 감이 아니라 **매번 같은
    방식으로** 내린다는 것이 핵심입니다.

15. (선택, 30초) **MLflow** 를 엽니다 — experiment `rhsummit-model-validation`.

    *멘트:* 측정 결과가 여기 쌓입니다. 다음에 새 버전이 들어오면 같은 자리에서
    비교합니다.

### 촬영 전 반드시 확인할 것

**실행 버튼만 촬영하고 결과는 미리 돌려둔 것을 쓰세요.** 1회 실행에 30~50분입니다
(35 GiB 다운로드 4분, vLLM 로딩 5분, 측정 5분, 기준 모델 복원 5분 + 대기).
카메라 앞에서 기다릴 구간이 아닙니다.

**게이트 기준이 어디서 나왔는지 말하세요.** 30 tok/s 와 200 ms 는 임의로 정한 값이
아니라 **기준 모델의 실측치에서 나온 숫자**입니다. 이 말을 빼면 "통과/기각"이
임의로 들립니다.

> ⚠️ **조건이 같지 않다는 단서를 반드시 붙이세요.** 35B는 48 GB 카드에서 OOM을 피하려고
> `--enforce-eager`(CUDA 그래프 끔)로 띄웠고 32B는 런타임 기본값으로 돌았습니다.
> 그래서 이것은 **모델 대 모델 비교가 아니라 "이 GPU 한 장에서 각 모델이 실제로 낼 수
> 있는 성능" 비교**입니다.
>
> 데모 메시지로는 오히려 이쪽이 정확합니다. 청중이 궁금한 것은 논문상 어느 모델이
> 빠른가가 아니라 **"내 GPU에서 이 모델을 쓸 수 있나"** 입니다. 질문이 나오면 이렇게
> 답하세요 — 35B는 MoE라 이론상 더 빨라야 하는데, 가중치 35 GiB를 올리면 KV 캐시 여유가
> 없어져 성능을 포기해야 하고 그 대가가 3.7배였습니다. **양자화 방식과 VRAM 여유가
> 파라미터 수보다 결과를 더 크게 좌우합니다.**

## Part 6 — 실제로 서빙되는지 (약 1분)

16. OpenAI 호환 엔드포인트를 호출합니다.

```bash
curl -sk https://<endpoint>/v1/chat/completions \
  -H 'Content-Type: application/json' \
  -d '{"model":"qwen3-32b-awq",
       "messages":[{"role":"user","content":"Red Hat OpenShift AI를 두 문장으로 설명해줘."}],
       "max_tokens":200,
       "chat_template_kwargs":{"enable_thinking":false}}'
```

> ⚠️ **`enable_thinking: false` 를 빼지 마세요.** Qwen3 계열은 추론 모델이라 답하기
> 전에 사고 과정을 출력합니다. 빼면 화면에 `<think> Okay, the user wants me to...`
> 가 쏟아지고, 실측으로 **200토큰을 사고에만 쓰고 답을 시작조차 못 했습니다.**
> 끄면 같은 질문에 88토큰으로 깔끔한 두 문장이 나옵니다.

   *멘트:* OpenAI 호환이라 기존 클라이언트 코드를 그대로 이쪽으로 돌리면 됩니다.

## Part 7 — 비용과 용량 (약 1분)

17. OpenShift 콘솔 → **Observe → Dashboards → NVIDIA GPU / DCGM**.
18. 프레임버퍼와 텐서 파이프 활성도를 보여줍니다.
19. **Observe → Alerting** 에서 `GPU` 검색 → `GPUNodeUnallocated`.

   *멘트:* 이 카드는 누가 쓰든 안 쓰든 시간당 과금됩니다. 그래서 플랫폼이 조용히
   청구되게 두지 않고 놀고 있다고 알려줍니다.

---

## 촬영 중 CLI 확인

```bash
oc -n <PROJECT> get inferenceservice
oc -n <PROJECT> get pods -l serving.kserve.io/inferenceservice
oc get node -l node-role.kubernetes.io/gpu \
  -o jsonpath='{.items[0].status.allocatable.nvidia\.com/gpu}{"\n"}'
```

파이프라인 실행을 확인할 때:

```bash
oc -n <PROJECT> get workflows.argoproj.io
oc -n <PROJECT> get pods | grep model-intake-validation
```

> **파드 개수를 세세요.** 성공한 단계 수와 `...-container-impl-...` 파드 수가 다르면
> KFP 캐시가 태스크를 건너뛴 것입니다. 실제로 배포하지 않은 채 "성공"으로 보고된 적이
> 있습니다. 미리 돌려둔 실행을 쓸 때도 이걸 한 번 확인하고 촬영에 넣으세요.

## 편집 노트

콘솔 데모가 심심한 주된 이유는 화면이 작아서가 아니라 **기다리는 시간** 때문입니다.
모델을 미리 S3에 넣어둔 것도 그래서입니다.

- 첫 vLLM 기동 4~10분 → **컷하고 자막**
- 파드가 Running 되는 것을 지켜보는 장면 → 전부 컷
- 폼 입력은 1.5~2배속
- **파이프라인 1회 실행 30~50분 → 실행 버튼만 찍고 미리 돌려둔 결과로 점프**

파이프라인 구간은 "실행 → (컷) → 완료된 그래프"로 붙이는 것이 가장 깔끔합니다.
그래프에서 `promote` 가 회색이고 `teardown` 이 초록인 대비가 이 데모의 그림입니다.

확대가 필요한 구간은 메타데이터 입력 폼, 하드웨어 프로파일 드롭다운, OOM 오류 메시지,
두 버전이 나란히 보이는 목록, **`gate` 단계의 REJECTED 로그**, 두 모델 측정값 비교,
GPU 대시보드 그래프입니다.

## 솔직한 단서

**내비게이션 문구는 확인하지 못했습니다.** RHOAI 3.5 왼쪽 메뉴의 정확한 텍스트 —
레지스트리가 **Models** 하위인지 최상위인지 — 는 대화형 로그인이 필요해 검증하지
못했습니다. 위의 모든 *값*은 클러스터에서 읽은 정확한 값입니다.

**리허설을 하세요.** 특히 첫 배포를 한 번 돌려두면 이미지가 노드에 캐시됩니다.
파이프라인도 한 번 완주시켜 두세요 — 컴포넌트 이미지(`ubi9/python-311`)를 처음 받을 때
CDN 전송이 끊겨 `ErrImagePull` 이 난 적이 있습니다(재시도로 통과합니다).

**Part 5의 수치는 실측값입니다.** 32B 36.87 tok/s / 105 ms, 35B 9.87 tok/s / 311 ms.
같은 조건 두 번 돌려 편차 0.01%로 재현됐습니다. 다만 **35B의 OOM 회피 인자 단서를
빼고 말하면 부정확한 주장이 됩니다** — Part 5의 경고 상자를 그대로 말하세요.
