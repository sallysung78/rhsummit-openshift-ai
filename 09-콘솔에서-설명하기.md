# 콘솔에서 설명하기 — CLI로 만든 것을 화면으로 보여주기

이 환경은 **전부 CLI와 YAML로 만들었습니다.** 그래서 "무엇을 했는지"는 알아도
"그게 화면 어디에 있는지"는 따로 익혀야 합니다. 이 문서는 그 간극을 메웁니다.

> **메뉴 라벨 확인 상태**: 아래 중 **Develop & train → Pipelines(Pipeline definitions ·
> Runs · Artifacts · Executions) · Experiments · Evaluations(Tech Preview)**, 그리고
> **AI hub**, **Projects**, **Applications**, **Settings**는 실제 화면(스크린샷)으로 확인한
> 라벨입니다. 모델 카탈로그·모델 레지스트리는 **AI hub** 아래에 있을 것으로 보이지만
> 그 하위 라벨은 직접 보지 못했습니다 — 리허설 때 확인하세요.

## 실제 왼쪽 메뉴 (RHOAI 3.5, 확인됨)

```
Home
Projects
AI hub                      ← 모델 카탈로그·레지스트리가 여기 (하위 라벨 미확인)
Develop & train
  Pipelines
    Pipeline definitions    ← 11단계 파이프라인
    Runs                    ← 실행 기록, gate 로그
    Artifacts
    Executions
  Experiments               ← MLflow (임베드). GenAI | Model training 토글 주의
  Evaluations  (Tech Preview) ← EvalHub
Learning resources
Applications
Settings
```

> **MLflow는 별도 앱이 아닙니다.** Experiments 메뉴가 곧 MLflow이고, experiment 상단의
> `GenAI | Model training` 토글에서 **Model training**을 골라야 run과 metric이 보입니다.
> → [08-mlflow-화면-설명.md](08-mlflow-화면-설명.md)

---

## 이 클러스터에 켜져 있는 UI 모듈

대시보드가 실제로 로드하는 모듈입니다. **숨겨진 메뉴는 없습니다.**

| 모듈 | 데모에서 쓰나 |
|---|---|
| `modelRegistry` | **씁니다** — 모델 버전과 이유 |
| `evalHub` | **씁니다** — 평가 실행과 결과 |
| `mlflow` / `mlflowEmbedded` | **씁니다** — 측정 기록 |
| `genAi`, `automl`, `autorag`, `agentOps`, `maas` | 이번 데모에서는 안 씁니다 |

> 메뉴가 많아 보여도 **우리가 열 것은 세 개**입니다. 나머지는 지나가세요.
> 질문이 나오면 "이 데모 범위 밖"이라고 답하면 됩니다.

---

## 화면 순서 — 5개 화면, 각각 무엇을 말할지

발표 3막과 대응시켰습니다.

### 1막 · 왜 이 두 모델인가

#### 화면 ① 모델 카탈로그

*여는 법*: **AI hub** → 모델 카탈로그 (하위 라벨은 리허설 때 확인).

*말할 것*: Red Hat이 검증한 모델 목록입니다. 여기서 후보를 고릅니다. **읽는 곳**이고
쓰는 곳이 아닙니다.

*짚을 것*: 목록에 양자화 방식과 크기가 적혀 있다는 점. 그게 다음 판단의 재료입니다.

#### 화면 ② 모델 레지스트리

*여는 법*: **AI hub** → 모델 레지스트리 → `rhsummit-registry` → `qwen-demo`.

*말할 것*: 카탈로그가 서점이라면 **레지스트리는 우리 책장과 대출 기록**입니다.
우리가 무엇을 쓰기로 했고, 누가 넣었고, 어느 버전이 운영 중인지가 남습니다.

*짚을 것*: 버전 두 개가 나란히 있고 상태가 다릅니다.

| 버전 | 양자화 | 크기 | 상태 |
|---|---|---|---|
| `v1-35b-fp8` | FP8 | 34.9 GiB | **retired — VRAM 제약** |
| `v2-32b-awq` | AWQ 4비트 | 18.0 GiB | **active** |

> **여기가 1막의 핵심 장면입니다.** "왜 검증된 모델을 안 쓰고 저걸 쓰냐"는 질문에
> 대한 답이 화면에 기록으로 남아 있다는 것을 보여주세요.

### 2막 · 재 봤더니 무엇이 문제였나

#### 화면 ③ 배포된 모델

*여는 법*: 데이터 사이언스 프로젝트 → 해당 프로젝트 → 모델 서빙 탭.

*말할 것*: 지금 돌고 있는 것은 32B입니다. 35B는 올라가지 않았습니다.

*짚을 것*: 하드웨어 프로파일이 지정되어 있다는 점. GPU 노드에 테인트가 걸려 있어
이걸 안 고르면 파드가 `Pending`에서 멈춥니다.

> **OOM 오류는 콘솔에서 보여주기 어렵습니다.** 이미 지나간 이벤트라 화면에 남아 있지
> 않습니다. **터미널이나 캡처 이미지를 준비**하세요. 콘솔로 억지로 찾으려 하면 시간만
> 갑니다.

#### 화면 ④ EvalHub

*여는 법*: **Develop & train → Evaluations** (Tech Preview 배지).

*말할 것*: 평가 도구를 REST API 하나로 묶어 줍니다. 직접 구현하지 않았습니다.
GuideLLM으로 성능을 쟀습니다.

*짚을 것*: 평가 작업 목록과 상태. 완료된 작업의 결과.

### 3막 · 매번 하려면

#### 화면 ⑤ 파이프라인

*여는 법*: **Develop & train → Pipelines → Runs** → `validation-…` 실행 → 그래프. 정의는 **Pipeline definitions → `model-intake-validation`**.

*말할 것*: 앞에서 손으로 한 일을 그대로 자동화했습니다. 배포하고, 불러 보고, 재고,
판정하고, 되돌립니다.

*짚을 것*: 그래프에서 **`promote`가 회색이고 `teardown`이 초록**인 대비.
기각됐지만 환경은 복구됐습니다.

#### 화면 ⑥ MLflow (선택)

→ [08-mlflow-화면-설명.md](08-mlflow-화면-설명.md)에 따로 정리했습니다.
**차트 탭은 비어 있으니 열지 마세요.**

---

## 콘솔로 보여주기 어려운 것 — 미리 준비할 자료

CLI로 만든 것 중 **화면에 안 남는 것**이 있습니다. 무대에서 찾지 말고 미리 캡처하세요.

| 보여주고 싶은 것 | 콘솔에 있나 | 대안 |
|---|---|---|
| CUDA OOM 오류 | ❌ 지나간 이벤트 | **캡처 이미지 준비** |
| `gate`의 REJECTED 로그 | ⭕ 파이프라인 실행 로그 | 클릭 경로를 미리 연습 |
| 측정 수치 5개 | △ MLflow 아티팩트 JSON | **슬라이드 표가 더 낫습니다** |
| 파이프라인 YAML·컴포넌트 코드 | ❌ | 보여줄 필요 없음 |
| DB·PVC·오퍼레이터 구성 | △ OpenShift 콘솔 | **슬라이드로 대체** |

> **전부 콘솔에서 보여주려 하지 마세요.** 콘솔이 잘하는 것은 "상태와 기록"이고,
> 숫자 비교와 구조 설명은 슬라이드가 낫습니다. **화면 전환이 곧 설명의 리듬**입니다.

---

## 리허설 체크리스트

CLI로만 작업했다면 이 항목들을 **반드시 한 번씩 직접 눌러 보세요.**

- [ ] 왼쪽 메뉴에서 모델 카탈로그·모델 레지스트리·EvalHub가 각각 **어디 있는지**
- [ ] 레지스트리에서 `qwen-demo`의 **두 버전이 한 화면에 보이는지**
- [ ] 프로젝트 → 모델 서빙에서 배포 상태가 보이는지
- [ ] 파이프라인 실행 기록에서 **`gate` 로그까지 가는 클릭 수**
- [ ] MLflow에서 Artifacts 탭까지 가는 경로
- [ ] 로그인 후 첫 화면에서 각 목적지까지 **몇 번 클릭인지 세어 보기**

> 마지막 항목이 중요합니다. 클릭이 네 번 넘어가면 그 구간은 **미리 열어 둔 탭으로
> 전환**하는 편이 낫습니다. 무대에서 메뉴를 더듬는 장면이 데모를 가장 크게 망칩니다.

## 브라우저 탭을 미리 열어 두세요

각 화면을 탭으로 열어 두고 **탭 전환으로 진행**하면 메뉴를 찾는 시간이 사라집니다.

```
탭1 모델 카탈로그   탭2 레지스트리(qwen-demo)   탭3 모델 서빙
탭4 EvalHub        탭5 파이프라인 실행 기록      탭6 MLflow 아티팩트
```

로그인 세션이 만료되지 않도록 시작 직전에 한 번씩 새로고침하세요.
