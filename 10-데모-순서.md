# 데모 순서 — 반입 · 검증 · 자동화

세 부분입니다. 각 부분에 **어느 메뉴를 열고 무엇을 말할지**를 붙였습니다. 메뉴 라벨은
실제 화면에서 확인한 것만 굵게 썼고, 확인하지 못한 하위 라벨은 그렇다고 적었습니다.

```
1. 모델 반입     커뮤니티 모델 vs Red Hat 검증 모델 — 검증 모델은 가속기별 성능값이 미리 있다
2. 반입 후 검증  올려 보고 · 불러 보고 · 재고 · 기준과 비교한다
3. 자동화        2 를 파이프라인으로 매번 같게 — 결과는 Experiments(MLflow)에 쌓인다
```

---

## 1. 모델 반입 — 두 종류의 모델 (약 4분)

### 화면 ① 허깅페이스 — 커뮤니티 원본

`Qwen/Qwen3.6-35B-A3B`. 크기 **67 GiB**, BF16. 모델 카드에 하드웨어 정보가 없습니다.

*멘트:* 커뮤니티 모델은 "무엇으로 어디까지 되는가"를 **우리가 직접 알아내야** 합니다.
받아서 올려 보기 전엔 모릅니다.

### 화면 ② OpenShift AI 모델 카탈로그 — Red Hat 검증 모델

메뉴: **AI hub** → 모델 카탈로그 (하위 라벨은 리허설 때 확인). `Red Hat AI validated`
라벨로 필터 → `RedHatAI/Qwen3.6-35B-A3B-FP8`.

같은 모델의 FP8 양자화판입니다. **35 GiB.** 카탈로그에는 허깅페이스 카드에 없는 것이
있습니다 — **가속기별 성능 측정값**입니다.

| 카탈로그가 알려주는 것 | 값 (이 모델) |
|---|---|
| 가중치 크기 | 37.5 GB |
| **최소 VRAM** | **43.1 GB** |
| 검증 가속기 | **A100-80 · H100 · H200** (각 1·2·4장) |
| 시나리오 | chatbot · code_fixing · rag · long_rag |
| 측정 도구 | GuideLLM, 고정 부하 1~9 rps, p50–p99 |
| 검증 스택 | RHOAI 3.5 · vLLM 0.21 |

*멘트:* 검증 모델은 **정확도**(양자화해도 원본 대비 99~100%)와 **가속기별 성능**을 미리
알려줍니다. 우리가 다시 재지 않아도 되는 부분입니다.

> ★ **여기서 한 박자 멈추세요.** 목록에 **L40S가 없습니다.** 우리 GPU는 L40S 48 GB 한 장이고,
> 최소 VRAM은 43.1 GB입니다. 즉 이 모델은 **우리 가속기에서는 검증되지 않았고**, 여유가
> 5 GB 남짓입니다. 카탈로그가 이걸 미리 말해 주고 있습니다 — **읽었다면 미리 알았을
> 일입니다.** 2번 검증이 필요한 이유가 여기서 나옵니다.

### 공급망 한 마디 (30초)

*멘트:* 이 런타임은 `--trust-remote-code`를 기본으로 켭니다. 모델을 **어디서** 받느냐가 곧
**무엇을 실행하느냐**입니다. 검증 모델은 registry.redhat.io의 컨테이너 이미지로 오고, 레드팀
평가 항목이 붙어 있습니다.
→ [13-모델-공급망-보안.md](https://github.com/sallysung78/rhsummit-openshift-ai/blob/main/13-%EB%AA%A8%EB%8D%B8-%EA%B3%B5%EA%B8%89%EB%A7%9D-%EB%B3%B4%EC%95%88.md)

### 화면 ③ 모델 레지스트리 — 우리가 들여온 것

메뉴: **AI hub** → 모델 레지스트리 → `rhsummit-registry` → `qwen-demo`.

| 버전 | 출처 | 양자화 | 크기 | 상태 |
|---|---|---|---|---|
| `v1-35b-fp8` | Red Hat 검증 | FP8 | 34.9 GiB | retired — VRAM 제약 |
| `v2-32b-awq` | 커뮤니티 | AWQ 4비트 | 18 GiB | active |

*멘트:* 카탈로그는 **읽는 곳**, 레지스트리는 **쓰는 곳**입니다. 우리가 무엇을 들여왔고
왜 어느 것을 쓰는지가 여기 남습니다.

---

## 2. 반입 후 검증 — 무엇을 검증할 수 있나 (약 5분)

> **먼저 범위를 말하세요.** 검증은 0~10 사다리이고 플랫폼팀 몫은 0~6입니다. 편향·가드레일
> (7~10)은 서비스팀 몫이라 이 데모에서 다루지 않습니다 — 도구는 이미 있습니다.
> → [12-검증-범위.md](https://github.com/sallysung78/rhsummit-openshift-ai/blob/main/12-%EA%B2%80%EC%A6%9D-%EB%B2%94%EC%9C%84.md)

여기가 시나리오의 핵심입니다. **네 단계**이고, 각 단계에 "실제로 겪은 일"이 하나씩
붙어 있습니다.

| 검증 | 묻는 것 | 도구 | 우리가 겪은 것 |
|---|---|---|---|
| ① 올라가나 | 가속기에 들어가나 | 배포 (KServe) | 커뮤니티 원본 67 GiB → **가중치 적재 중 실패** · 검증 FP8 35 GiB → OOM, 인자 조정 후 기동 |
| ② 답하나 | 추론이 되나 | 요청 1회 | `enable_thinking:false` 안 주면 `<think>`만 200토큰 |
| ③ 얼마나 빠르나 | 처리량·지연 | **Evaluations**(EvalHub, GuideLLM) | 32B 36.9 tok/s vs 35B 9.9 tok/s |
| ④ 기준을 넘나 | 통과/기각 | 임계값 비교 | 35B 기각 (30 tok/s · 200 ms 기준) |

### 화면 ④ 배포 — 올라가나

메뉴: **Projects** → `rhsummit` → 모델 서빙 탭. 현재 돌아가는 것은 `qwen3-32b-awq`.

*멘트:* 첫 검증은 단순합니다 — **올라가나.**

| | 커뮤니티 원본 (BF16) | Red Hat 검증 (FP8) |
|---|---|---|
| 크기 | 67 GiB | 35 GiB |
| 실패 지점 | **가중치 적재 도중** | 가중치 적재 후, CUDA graph 준비 단계 |
| 메모리 | 43.74 GiB 채우고 512 MiB 더 요청 → 119 MiB 남음 | 1.03 GiB 요청 → 1001 MiB 남음 |
| 회피 | **불가** — 가중치 자체가 안 들어감 | 가능 — `--enforce-eager` 등으로 기동, 대신 3.7배 느림 |
| 경과 | 다운로드 509초, 재시작 2회 → CrashLoopBackOff, 총 13분 | — |

vLLM이 남긴 메시지가 결론입니다:

```
Failed to load model - not enough GPU memory. Try lowering --gpu-memory-utilization
to free memory for weights, increasing --tensor-parallel-size, or using --quantization.
```

*멘트:* **"`--quantization`을 쓰라"고 vLLM이 직접 말합니다. 그게 Red Hat 검증판입니다.**
같은 실패를 두 번 재현했습니다(15:46, 15:59).

> OOM 메시지는 지나간 이벤트라 콘솔에 없습니다. **캡처를 준비**하세요.

### 화면 ⑤ Evaluations — 얼마나 빠르나

메뉴: **Develop & train → Evaluations** (Tech Preview).

*멘트:* 성능 평가는 직접 구현하지 않았습니다. EvalHub가 GuideLLM을 REST API로 묶어
줍니다. 32B 네 번 측정이 36.87·36.87·36.89·36.90 — **측정이 재현됩니다.** 그 옆 35B는
9.87.

### 판정 — 기준을 넘나

*멘트:* 기준은 기준 모델의 실측치입니다(30 tok/s, 200 ms). 35B는 둘 다 미달. 이 판정을
사람 감이 아니라 **매번 같은 방식으로** 내리는 게 3번입니다.

> ⚠️ 35B 수치를 말할 때 **`--enforce-eager`로 띄웠다는 단서**를 빼지 마세요.
> 카탈로그 H100 1장 chatbot은 TTFT 101~118 ms입니다. 우리 L40S는 311 ms — 가속기가
> 다르고 인자도 다릅니다. "이 GPU 한 장에서 실제로 낼 수 있는 성능" 비교입니다.

---

## 3. 자동화 — 파이프라인과 Experiments (약 4분)

**질문에 먼저 답하면**: 파이프라인은 MLflow가 아닙니다. **파이프라인(Data Science
Pipelines)이 2번을 반복 실행**하고, **Experiments(MLflow)는 그 결과가 쌓이는 곳**입니다.
둘은 역할이 다릅니다.

### 화면 ⑥ Pipelines — 반복 실행

메뉴: **Develop & train → Pipelines → Pipeline definitions** → `model-intake-validation`.
그래프 8단계.

*멘트:* 2번에서 손으로 한 일 그대로입니다 — 올리고(deploy), 불러 보고(smoke),
재고(measure), 판정하고(gate), 통과하면 표시(promote), **무조건 되돌립니다**(teardown).
GPU가 한 장이라 기준 모델을 내리고 시작하고, 끝나면 복원합니다.

**Runs**로 이동 → 완료된 실행 → 그래프에서 **`promote` 회색 / `teardown` 초록**.

*멘트:* 기각됐는데 환경은 원래대로입니다. 검증이 운영을 망가뜨리지 않습니다.

### 화면 ⑦ Experiments — 결과가 쌓이는 곳

메뉴: **Develop & train → Experiments** → `rhsummit-model-validation` →
상단 토글 **`Model training`** (기본 `GenAI`는 비어 있음 — 정상).

- 실행 목록: metric 열 `output_tokens_per_second`, `mean_ttft_ms`, `gate_pass` — 32B 네 줄과
  35B 한 줄이 나란히
- 차트 뷰: 막대 높이 1/4 차이
- run Overview → Parameters: `vllm_args`, `gpu` — **조건이 함께 기록**

*멘트:* 새 버전이 들어오면 파이프라인을 한 번 돌리고, 같은 표에서 비교합니다.
"이 모델을 쓸 수 있나"에 매번 같은 방식으로 답합니다.

→ 상세: [08-mlflow-화면-설명.md](08-mlflow-화면-설명.md)

---

## 열지 않을 것

| 메뉴 | 이유 |
|---|---|
| Experiments의 `GenAI` 토글 화면 | trace가 없어 비어 있음 |
| MLflow의 Models / Prompts | 안 씀. 모델 버전은 OpenShift AI 레지스트리에서 |
| AI hub의 다른 항목 (GenAI, AutoML 등) | 이 데모 범위 밖 |

## 리허설 체크

- [ ] AI hub 하위에서 카탈로그·레지스트리 라벨 확인, `Red Hat AI validated` 필터
- [ ] 카탈로그에서 Qwen3.6-35B-A3B-FP8의 **성능 탭에 A100/H100/H200만 있는 것** 확인
- [ ] Experiments 토글을 `Model training`으로 두고 시작
- [ ] Pipelines → Runs에서 promote/teardown 대비가 보이는 실행 찾아 두기
- [ ] 커뮤니티 원본 실패 로그 캡처 — 위 vLLM 메시지 두 줄
