# 빈 AWS 계정에서 데모 가능한 OpenShift AI까지 — 전체 순서

한 번에 따라갈 수 있는 체크리스트입니다. 각 단계의 배경과 겪은 함정은 번호가 붙은
개별 문서에 있고, 여기서는 **무엇을 어떤 순서로 하고 무엇으로 확인하는지**만 다룹니다.

전부 처음부터 하면 **약 4시간**이고 대부분은 기다리는 시간입니다.

```
0  사전 준비         30분   계정·쿼터·pull secret 확인
1  OpenShift 설치    45분   ← 대기가 긴 구간
2  OpenShift AI      25분
3  GPU 노드          30분   ← 용량 확보가 변수
4  GPU 관측          10분
5  오브젝트 스토리지   15분
6  모델 S3 적재       20분   ← 클러스터 안에서 받아야 함
7  프로젝트와 연결     5분
8  파이프라인 서버    10분
9  모델 레지스트리    10분   ← 컴포넌트와 별개로 인스턴스가 필요
10 모델 등록·배포     20분
11 검증 파이프라인   40분   ← 한국어를 못 쓰는 게 함정
12 EvalHub 평가      20분   ← 성능 숫자를 뽑는 구간
13 MLflow 연동       15분   ← CRD가 클러스터 범위인 게 함정
```

범례: ✅ 완료 · ⬜ 예정

---

## 0. 사전 준비 ✅

빈 AWS 계정이라도 IPI 설치는 네 가지를 전제합니다.

| 항목 | 확인 |
|---|---|
| Route 53 **퍼블릭** 호스티드 존 | `aws route53 list-hosted-zones` |
| Red Hat pull secret | [console.redhat.com](https://console.redhat.com/openshift/install/pull-secret) |
| IAM 자격증명 | `aws sts get-caller-identity` |
| 쿼터 | Standard vCPU ≥ 36, **Elastic IP ≥ 3** |

Elastic IP가 조용한 병목입니다. AZ마다 NAT 게이트웨이가 하나씩 생기고 각각 EIP를
먹는데 기본 한도가 5입니다.

**리전 선택이 나중의 GPU를 결정합니다.** 클러스터 생성 후에는 바꿀 수 없고 GPU 노드는
같은 VPC에 있어야 합니다. 쓰려는 GPU 타입이 3개 AZ 모두에 제공되는 리전을 고르세요.

→ 자세히: [1단계 문서](01-openshift-설치.md)

## 1. OpenShift 설치 ✅ — 45분

```bash
source scripts/00-env.sh
./scripts/01-create-cluster.sh
```

42분 걸립니다. 그 전에 30초로 설정 오류를 잡을 수 있습니다:

```bash
openshift-install create manifests --dir <디렉터리>   # 리소스 생성 없이 검증만
```

**확인**

```bash
oc get clusterversion                      # Available=True
oc get nodes                               # 6대 Ready
oc get co | grep -v 'True.*False.*False'   # 비어 있으면 정상
```

⚠️ `cluster/` 디렉터리를 지우지 마세요. 나중에 삭제할 때 필요합니다.

## 2. OpenShift AI 설치 ✅ — 25분

**순서가 중요합니다. RWX 스토리지가 먼저입니다.**

```bash
./scripts/03-efs-setup.sh        # EFS + 마운트 타깃 + StorageClass
./scripts/04-verify-catalog.sh   # ★ 적용 전에 채널 대조
./scripts/05-install-rhoai.sh
```

**`04-verify-catalog.sh`를 건너뛰지 마세요.** RHOAI의 `stable` 채널은 아직 2.x를
가리킵니다. 3.x를 쓰려면 `stable-3.5`처럼 명시해야 하고, 2.x와 3.x는 API가 달라
매니페스트가 그대로 적용되지 않습니다.

**확인**

```bash
oc get datasciencecluster default-dsc -o jsonpath='{.status.phase}'   # Ready
oc -n redhat-ods-applications get pods                                 # 21개 내외
oc -n openshift-ingress get route data-science-gateway                 # rh-ai.apps...
```

→ 자세히: [2단계 문서](02-openshift-ai-설치.md)

## 3. GPU 노드 연결 ✅ — 30분

```bash
./scripts/06-gpu-find-capacity.sh g6e.2xlarge:<AZ-a> g6e.2xlarge:<AZ-b> g6e.2xlarge:<AZ-c>
oc apply -f manifests/05-machineset-gpu.yaml
./scripts/07-gpu-operators.sh    # NFD + NVIDIA GPU Operator
```

**용량이 가장 큰 변수입니다.** H200·H100은 온디맨드 용량이 없을 수 있고, AWS가
알려주는 "다른 AZ를 쓰라"는 안내는 신뢰할 수 없습니다 — 실제로 기동해 봐야 압니다.
탐색 스크립트는 실패한 시도를 정리하므로 재시도 비용이 없습니다.

**확인**

```bash
oc get nodes -l node-role.kubernetes.io/gpu \
  -o custom-columns='NODE:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu'
oc get clusterpolicy    # ready
```

> `allocatable`이 **`0`이면 준비 안 된 것**입니다. 값이 있는지가 아니라 **0보다 큰지**를
> 봐야 합니다. 드라이버 재빌드에 5~10분 걸립니다.

→ 자세히: [3단계 문서](03-gpu-노드-연결.md)

## 4. GPU 관측 ✅ — 10분

```bash
./scripts/09-gpu-observability.sh
```

대시보드(Observe → Dashboards → NVIDIA GPU / DCGM), 콘솔 링크, 비용·건강 알림 6종을
추가합니다.

**GPU 메트릭에는 user workload monitoring이 필요 없습니다.** GPU Operator가 자기
네임스페이스에 `openshift.io/cluster-monitoring=true`를 붙이기 때문입니다.

⚠️ 플랫폼 메트릭을 질의하는 알림은 **반드시 플랫폼 모니터링 네임스페이스**에 두세요.
사용자 네임스페이스에 두면 UWM이 셀렉터에 `namespace="..."`를 주입해 **조용히 발동하지
않습니다.** 오류도 안 납니다.

## 5. 오브젝트 스토리지 ✅ — 15분

모델과 파이프라인 아티팩트를 둘 곳이 필요합니다.

```bash
aws s3api create-bucket --bucket <INFRA_ID>-models --region <REGION> \
  --create-bucket-configuration LocationConstraint=<REGION>
aws s3api put-public-access-block --bucket <INFRA_ID>-models \
  --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
aws s3api put-bucket-encryption --bucket <INFRA_ID>-models \
  --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'
```

프리픽스는 `models/`와 `pipelines/` 둘로 나눕니다.

⚠️ **OpenShift 내부 레지스트리 버킷(`<INFRA_ID>-image-registry-...`)을 재사용하지
마세요.** 레지스트리 오퍼레이터 소유라 모델 아티팩트가 정리될 수 있습니다.

**admin 키를 쓰지 말고 전용 IAM 사용자를 만드세요.** 이 버킷에만 권한을 주고, 다른
버킷·IAM·EC2에서 실제로 거부되는지 확인하세요.

## 6. 모델을 S3에 적재 ✅ — 20분

**노트북이 아니라 클러스터 안에서 받으세요.** 18 GiB를 가정용 회선으로 내려받는 건
방향이 틀렸습니다. 클러스터에서 돌리면 HF→S3 양쪽 다 AWS 네트워크입니다.

```yaml
resources:
  # emptyDir 은 ephemeral-storage 한도에 산입된다.
  # 가중치보다 넉넉히 잡지 않으면 파드가 Evicted 된다.
  requests: { ephemeral-storage: 30Gi }
  limits:   { ephemeral-storage: 50Gi }
```

이 한도를 4Gi로 뒀다가 18 GiB 모델에서 세 번 연속 퇴거당했습니다.

**확인**

```bash
aws s3 ls --summarize --recursive s3://<INFRA_ID>-models/models/
```

## 7. 프로젝트와 연결 ✅ — 5분

RHOAI 대시보드에서 Data Science Project를 만들고 S3 연결을 추가합니다.
연결 유형은 **`S3 compatible object storage - v1`** 입니다.

시크릿을 직접 만들어도 됩니다 — 대시보드가 인식하려면 라벨과 어노테이션이 필요합니다:

```yaml
labels:
  opendatahub.io/dashboard: "true"
  opendatahub.io/managed: "true"
annotations:
  opendatahub.io/connection-type-ref: s3
data 키: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_S3_ENDPOINT
        / AWS_DEFAULT_REGION / AWS_S3_BUCKET
```

⚠️ **CLI로 연결을 만들면 어노테이션 하나를 놓치기 쉽습니다.**

```yaml
opendatahub.io/connection-type-ref: s3   # 대시보드 UI 용
opendatahub.io/connection-type: s3       # odh-model-controller 용 ← 이것도 필요
```

뒤엣것이 없으면 컨트롤러가 `storage-config` 시크릿의 항목을 `type: ""` 으로 만들고,
나중에 모델을 배포할 때 웹훅이 이렇게 거부합니다:

```
storage type must be one of [s3, hdfs, webhdfs]. storage type [] is not supported
```

**오류 메시지가 원인을 전혀 가리키지 않습니다.** 연결 시크릿의 어노테이션을 의심하기까지
한참 걸립니다. 대시보드로 만들면 둘 다 붙습니다.

**촬영이 있다면 연결을 미리 만들어 두세요.** 카메라 앞에서 시크릿 키를 타이핑하지
않게 됩니다.

## 8. 파이프라인 서버 ✅ — 10분

파이프라인은 **프로젝트마다 자기 서버**가 있어야 합니다. 컴포넌트가 Managed인 것과
별개입니다.

`spec`에서 **필수는 `objectStorage` 하나**입니다. Argo가 스텝 사이 아티팩트를 파드
파일시스템이 아니라 오브젝트 스토리지로 넘기기 때문입니다.

```bash
oc apply -f manifests/70-pipeline-server.yaml
```

5단계 버킷을 `basePath: pipelines`로 재사용하고, 자격증명은 7단계 연결을 그대로
참조합니다. DB는 지정하지 않으면 네임스페이스 안에 MariaDB가 생성됩니다.

**확인** — 파드가 떴다는 것만으로는 부족합니다. operator 자체 검증을 보세요:

```bash
oc -n <PROJECT> get dspa dspa -o json | jq -r '.status.conditions[]|"\(.type)=\(.status)"'
#   DatabaseAvailable=True
#   ObjectStoreAvailable=True     ← S3 연결이 실제로 검증된 것
#   Ready=True
```

`WebhookReady=False`와 `ManagedPipelineValid=False`는 정상입니다 — 각각 S3 저장소를
쓸 때 해당 없음, 샘플 파이프라인을 끈 결과입니다.

## 9. 모델 레지스트리 인스턴스 ✅ — 10분

**DSC의 `modelregistry` 컴포넌트가 `Managed`인 것과 레지스트리가 있는 것은 다른
얘기입니다.** 컴포넌트는 오퍼레이터와 `model-catalog`만 띄웁니다. 모델을 등록할
레지스트리는 `ModelRegistry` CR로 따로 만들어야 합니다.

```bash
oc get modelregistries.modelregistry.opendatahub.io -A   # 비어 있으면 인스턴스 없음
oc apply -f manifests/71-model-registry.yaml
```

`postgres.generateDeployment: true` 로 두면 오퍼레이터가 전용 Postgres도 함께 띄웁니다.

### 카탈로그와 레지스트리는 다릅니다

| | 모델 카탈로그 | 모델 레지스트리 |
|---|---|---|
| 성격 | **읽는 곳** — 남이 만든 모델 목록 | **쓰는 곳** — 우리가 쓰는 모델 대장 |
| 질문 | "어떤 모델이 있나?" | "우리가 뭘 쓰나? 누가 넣었나? 어느 버전이 운영 중인가?" |
| 개수 | 플랫폼에 하나 | 팀·용도별로 여러 개 |

**확인**

```bash
POD=$(oc -n rhoai-model-registries get pods --no-headers | awk '/^<레지스트리명>-[0-9a-f]/{print $1}' | head -1)
oc -n rhoai-model-registries exec "$POD" -c rest-container -- \
  curl -s -o /dev/null -w '%{http_code}\n' http://localhost:8080/api/model_registry/v1alpha3/registered_models
# 200
```

## 10. 모델 등록과 배포 ⬜ — 20분

Model Registry에 등록하고 KServe로 배포합니다.

| 고를 값 | |
|---|---|
| 서빙 런타임 | `vLLM NVIDIA GPU ServingRuntime for KServe` |
| 모델 포맷 | `vLLM` |
| 하드웨어 프로파일 | GPU 프로파일 (예: `NVIDIA L40S (1 x 48GB)`) |

⚠️ **하드웨어 프로파일은 선택 사항이 아닙니다.** GPU 노드에 테인트가 걸려 있고
대응하는 톨러레이션을 이 프로파일이 들고 있습니다. 잘못 고르면 파드가 `Pending`에
머뭅니다.

첫 vLLM 기동은 **5~10분**(이미지 풀 + 가중치 로드), 이후는 2~3분입니다.

### 모델 크기가 VRAM에 맞는지 먼저 계산하세요

가중치가 들어간다고 끝이 아닙니다. vLLM은 그 위에 **KV 캐시와 CUDA 그래프**를 올립니다.

48 GB 카드(실사용 44.4 GiB)에서 34.9 GiB 짜리 FP8 모델을 올렸더니 가중치는 다
로드되고 그 다음에서 죽었습니다:

```
torch.OutOfMemoryError: CUDA out of memory. Tried to allocate 1.03 GiB.
GPU has 44.39 GiB total, of which 1001.38 MiB is free.
  ← profile_cudagraph_memory
```

`--enforce-eager`(CUDA 그래프 끄기)와 `--max-model-len=4096`으로 기동은 시켰지만
컨텍스트가 짧고 느려 실용성이 낮았습니다. **가중치가 VRAM의 40%를 넘으면 다시
생각하세요.** 같은 카드에서 18 GiB 모델은 4분 만에 올라오고 컨텍스트 16K에 CUDA
그래프까지 켤 수 있었습니다.

### reasoning 모델은 thinking을 꺼야 합니다

Qwen3 계열 같은 추론 모델은 답하기 전에 사고 과정을 출력합니다. 데모에서는 화면이
지저분해지고 토큰도 낭비됩니다 — `max_tokens=200`으로 물었더니 사고 과정만 채우고
답을 시작조차 못 했습니다.

```json
"chat_template_kwargs": { "enable_thinking": false }
```

끄면 같은 질문에 88토큰으로 깔끔한 두 문장이 나옵니다.

**확인**

```bash
oc -n <PROJECT> get inferenceservice
curl -sk https://<endpoint>/v1/chat/completions -H 'Content-Type: application/json' \
  -d '{"model":"<모델명>","messages":[{"role":"user","content":"안녕"}]}'
```

→ 자세히: [4단계 문서](04-모델-배포-시나리오.md)

## 11. 검증 파이프라인 ✅ — 40분

**설계를 지배하는 제약은 GPU 개수입니다.** 1장이면 기준 모델과 후보를 동시에 띄울 수
없어 파이프라인이 직렬이어야 합니다.

```
preflight → release(기준 내림) → deploy(후보) → smoke → measure
          → gate → (통과: promote) → teardown(후보 내림 + 기준 복원)
```

`teardown` 은 `dsl.ExitHandler` 로 붙여 **통과/기각 무관하게 항상** 실행됩니다.
빼면 파이프라인이 유일한 GPU를 잡은 채 끝나 수동 데모가 막힙니다.

1회 실행에 30~50분이라 **실시간 촬영에는 맞지 않습니다.** 미리 돌려둔 결과를 보여주고
실행 버튼만 촬영하는 편집이 낫습니다.

```bash
./scripts/13-upload-pipeline.sh    # 컴파일 + 업로드(있으면 새 버전)
./scripts/14-run-pipeline.sh       # 실행 + 상태 추적
```

### ⚠️ 파이프라인 코드에 한국어를 쓸 수 없습니다

DSPA는 컴파일된 파이프라인 스펙을 MariaDB에 저장하는데 그 컬럼이 **latin1** 입니다.

```
pipelines          Description    latin1  latin1_swedish_ci
pipeline_versions  PipelineSpec   latin1  latin1_swedish_ci
```

KFP는 각 컴포넌트의 **소스 코드를 그대로** 스펙에 박아 넣습니다. 주석이든 로그
메시지든 파일에 한국어가 하나라도 있으면 업로드가 실패합니다. 업로드 시 넘기는
`name`/`description` 쿼리 파라미터도 마찬가지입니다.

**API는 원인을 알려주지 않습니다.**

```json
{"error_message":"Failed to create a pipeline and a pipeline version", ...}
```

`ds-pipeline-dspa` 파드 로그에만 진짜 원인이 있습니다.

```
Error 1366 (22007): Incorrect string value: '\xEB\xAA\xA8\xEB\x8D\xB8...'
```

그래서 파이프라인 코드는 영문으로 쓰고, 컴파일 직후 **ASCII 검사**를 넣어 서버에서
불투명한 에러를 받는 대신 컴파일 시점에 막습니다.

### KFP의 두 가지 제약이 구조를 바꿉니다

`release_gpu` 가 삭제한 스펙을 돌려주고 `teardown` 이 복원하는 게 자연스럽지만
KFP가 막습니다 — `exit_task cannot depend on any other tasks.` 그래서 기준 모델
스펙을 **파이프라인 파라미터**로 받습니다.

그 결과 `deploy_model` 이 `release_gpu` 의 출력을 더는 참조하지 않게 되는데,
**데이터 의존성이 사라지면 실행 순서도 사라집니다.** `dep.after(rel)` 을 명시해야
합니다. 빠뜨리면 기준 모델이 GPU를 잡은 상태로 후보 배포가 시작돼 Pending 에서
영원히 멈춥니다.

### 게이트 기준은 사람이 정해야 합니다

`min_output_tok_s`, `max_mean_ttft_ms` 는 파라미터입니다. 기본값을 임의로 박아 두면
"통과했다"는 말이 의미를 잃습니다. 데모에서는 기준 모델 실측치를 넣고 후보를 그와
비교하는 편이 설명하기 쉽습니다.

### 그 외

- **RBAC**: `pipeline-runner-dspa` 는 `inferenceservices` 권한을 **이미** 갖고 있습니다.
  부족한 건 `preflight` 의 nodes 읽기뿐이고, nodes는 클러스터 범위라 ClusterRole이
  필요합니다.
- **업로드 토큰**: kubeconfig가 admin 인증서면 `oc whoami -t` 가 실패합니다
  (`no token is currently in use`). 전용 SA에 `ds-pipeline-user-access-dspa` 롤을 묶어
  `oc create token` 으로 발급합니다.
- **TLS CA**: EvalHub 인증서 검증용 service CA는 모든 네임스페이스에 자동 주입되는
  `openshift-service-ca.crt` configmap에 있습니다. 컴포넌트 안에서 API로 읽으면
  `kfp-kubernetes`(볼륨 마운트)가 필요 없고 `verify=False` 로 도망갈 이유도 없습니다.
- **이미지 풀 실패**: 첫 실행에서 `ErrImagePull` 이 났는데 태그 오류가 아니라 quay CDN
  전송이 끊긴 것(`unexpected EOF`)이었습니다. kubelet 재시도로 통과합니다.
  메시지 전문을 읽기 전에 이미지 이름을 의심하지 마세요.

## 12. EvalHub 기반 평가 ✅ — 20분

**RHOAI 3.5에는 `EvalHub`가 GA로 들어 있습니다.** lm-evaluation-harness, GuideLLM 같은
평가 도구를 REST API 하나로 묶어주고 TrustyAI 오퍼레이터가 관리합니다. 평가를 직접
구현하기 전에 이것부터 보세요.

**켜는 법**: DSC에서 `trustyai`와 `mlflowoperator`를 `Managed`로 전환합니다.
`trustyai.eval.lmeval`의 `permitOnline`을 `allow`로 두어야 HF 데이터셋을 받습니다.
켜면 `evalhubs` / `lmevaljobs` / `mlflows` CRD가 올라옵니다.

⚠️ 대시보드의 `disableLMEval: false` 플래그만 켜면 **메뉴는 보이지만 동작하지
않습니다.** 백엔드인 `trustyai`가 함께 있어야 합니다.

```yaml
apiVersion: trustyai.opendatahub.io/v1        # v1alpha1 아닙니다
kind: EvalHub
metadata:
  name: evalhub
  namespace: <PROJECT>
spec:
  replicas: 1
  database: { type: sqlite }
  providers: [guidellm, lm-evaluation-harness]
  collections: [standard-llm-evals-v1]
  env:
    - name: MLFLOW_TRACKING_URI                # 13단계 참고. 오퍼레이터가 넣어주지 않습니다
      value: https://mlflow.redhat-ods-applications.svc:8443/mlflow
```

⚠️ **CRD 기본값의 `collections` 이름이 실제 제공 목록과 다릅니다.** 기본값 그대로
두면 존재하지 않는 컬렉션을 참조합니다. `/api/v1/evaluations/collections`로 실제
이름을 확인하고 명시하세요.

### API 접근에서 막히는 네 가지

**주소는 파드 내부 `8444`/HTTP입니다.** Service는 `8443`/HTTPS인데 파드 안에서
HTTPS로 8444를 치면 응답이 없습니다(`000`). 경로는 `/api/v1/evaluations/jobs`이고,
스펙 전문은 `/openapi.yaml`, 문서 UI는 `/docs`에 있습니다.

**인증은 헤더 두 개입니다.** `X-Tenant`와 `X-User`. EvalHub의 `auth.yaml`이
`namespace: "{{.FromHeader}}"`로 되어 있어 **테넌트 이름이 곧 네임스페이스**입니다.
`X-Tenant: default`를 보내면 RoleBinding이 `<PROJECT>`에 있으므로 `forbidden`이
납니다. 테넌트에 네임스페이스 이름을 넣으세요.

**모델 URL에 포트를 반드시 쓰세요.** KServe predictor Service는 headless
(`clusterIP: None`)입니다. DNS가 파드 IP를 직접 주기 때문에 Service의 포트 변환
(80 → 8080)이 일어나지 않습니다. 포트를 빼면 GuideLLM이 전부 실패하고 HTTP `000`을
받습니다. `http://<모델명>-predictor.<PROJECT>.svc.cluster.local:8080/v1`로 쓰세요.

**benchmark id는 부하 프로파일을 정하지 않습니다.** 벤치마크 정의에는 `metrics`,
`tags`, `primary_score`, `pass_criteria`만 있습니다. 실제 knob은 요청의
`parameters`입니다.

```bash
./scripts/12-evalhub-benchmark.sh                     # constant, rate=2, 120초
BENCH=constant RATE=4 MAX_SECONDS=300 ./scripts/12-evalhub-benchmark.sh
```

실측 결과 (Qwen3-32B-AWQ / L40S 1장, `constant` rate=2):

| 지표 | 값 |
|---|---|
| mean_ttft_ms | 104.96 |
| mean_itl_ms | 26.52 |
| output_tokens_per_second | 36.87 |
| prompt_tokens_per_second | 74.04 |
| requests_per_second | 0.28 |
| 판정 | pass (threshold 10) |

결과는 `results.benchmarks[].metrics`에 있습니다. `status.benchmarks[]`에는 상태만
있고 숫자가 없습니다 — 파싱 위치를 헷갈리기 쉽습니다.

> ⚠️ `parameters`의 `rate`/`max_seconds`가 그대로 반영되는지는 확인하지 못했습니다.
> 120초를 줬는데 작업은 약 256초 걸렸고, CLI로 `--rate 2 --max-seconds 120`을 직접
> 넘겼을 때는 같은 모델에서 248 tok/s가 나왔습니다. **절대 수치를 인용하기 전에
> 같은 조건으로 두 번 돌려 비교하세요.**

**진단은 sidecar 로그를 보세요.** 어댑터 컨테이너가 exit 137로 죽었을 때 OOM을
의심했지만 클러스터 전체에 `OOMKilling` 이벤트가 없었고 노드도 `MemoryPressure: False`
였습니다. 실제 원인은 sidecar 로그에 있었습니다.

> 정확도 벤치마크 전량 실행은 GPU 1장으로는 비현실적입니다(KMMLU만 35,030문항).
> 샘플을 제한하거나 성능 측정 위주로 쓰세요. 모델 관리가 주제라면 정확도 점수보다
> 성능 비교가 메시지에 맞습니다.

## 13. MLflow 연동 ✅ — 15분

EvalHub 결과를 MLflow에 남기면 "버전을 바꿨을 때 무엇이 좋아지고 무엇이 나빠졌는지"를
한 화면에서 비교할 수 있습니다. 데모에서 가장 설명하기 쉬운 장면입니다.

```yaml
apiVersion: mlflow.opendatahub.io/v1
kind: MLflow
metadata:
  name: mlflow
spec:
  replicas: 1
  backendStoreUri: sqlite:////mlflow/data/mlflow.db
  artifactsDestination: s3://<INFRA_ID>-models/mlflow
  serveArtifacts: true
  envFrom:
    - secretRef: { name: s3-models }
  storage:
    accessModes: [ReadWriteOnce]
    resources: { requests: { storage: 10Gi } }
```

⚠️ **`MLflow` CRD는 클러스터 범위입니다.** `metadata.namespace`를 써도 무시되고,
오퍼레이터는 **`redhat-ods-applications`에 배포합니다.** 이것 때문에 한참 헤맸습니다.

```bash
oc get crd mlflows.mlflow.opendatahub.io -o jsonpath='{.spec.scope}'   # → Cluster
```

**그래서 `envFrom`이 참조하는 시크릿도 `redhat-ods-applications`에 있어야 합니다.**
S3 시크릿을 모델 프로젝트에만 만들어 두면 마이그레이션 Job 파드가
`secret "s3-models" not found`로 죽고, CR은 아래 상태에서 멈춥니다:

```
[Available=False]  reason=MigrationRunning
  Waiting for migration Job mlflow-mg-XXXX-g1 to finish because
  status.version is empty during bootstrap
```

**이 상태의 진단이 까다롭습니다.** 오퍼레이터가 5분마다 "Migration requested"만
반복하고, 오퍼레이터 로그의 `namespace` 필드는 비어 있습니다. CR이 있는
네임스페이스에서 Job/PVC/이벤트를 찾으면 아무것도 없습니다.
**`redhat-ods-applications`의 이벤트를 보세요** — 거기에 진짜 실패 이유가 있습니다.

```bash
oc -n redhat-ods-applications get events --sort-by=.lastTimestamp | grep -i mlflow
```

시크릿을 복사하면 Job이 자동 재시도로 통과하고 CR이 `Migration=MigrationSucceeded`,
`Available=True`, `version: 3.14.0`이 됩니다.

### EvalHub가 MLflow를 못 찾는 이유

TrustyAI 오퍼레이터는 `MLFLOW_CA_CERT_PATH`, `MLFLOW_TOKEN_PATH`,
`MLFLOW_WORKSPACE`는 자동으로 주입하지만 **`MLFLOW_TRACKING_URI`는 넣지 않습니다.**
없으면 조용히 건너뜁니다:

```
warn  MLFlow tracking URI is not set, skipping MLFlow client creation
```

EvalHub CR의 `spec.env`에 직접 넣으세요(12단계 YAML 참고). 값은 MLflow CR의
`status.address.url`입니다. 성공하면 기동 로그가 이렇게 바뀝니다:

```
info  MLflow workspace support probed   workspaces_enabled=true
info  MLflow workspace configured       workspace=<PROJECT>
info  MLFlow tracking enabled
info  MLFlow request successful  GET /version  status=200
```

⚠️ **EvalHub를 `oc rollout restart`로 재기동하면 오퍼레이터가 되돌립니다.** 새
ReplicaSet은 만들어지지만 0으로 유지되고 기존 파드가 그대로 살아 있습니다.
**CR을 고쳐서 오퍼레이터가 재배포하게 하세요.**

### 기록 결과 확인

MLflow API는 **워크스페이스 컨텍스트를 요구합니다.** 헤더 없이 호출하면
`Workspace context is required for this request.`가 납니다. 워크스페이스는
네임스페이스와 같습니다.

```bash
curl -H "X-MLflow-Workspace: <PROJECT>" \
     -H "Authorization: Bearer $TOKEN" \
     -X POST .../api/2.0/mlflow/experiments/search -d '{"max_results":10}'
```

평가 작업에 `experiment` 블록을 넣으면 experiment가 생기고 응답에
`resource.mlflow_experiment_id`가 실립니다. 아티팩트는
`mlflow-artifacts:/workspaces/<PROJECT>/<id>/...` 아래 S3로 올라갑니다.

> **기대치를 낮춰 두세요.** EvalHub 3.5는 run을 만들고 `evaluation-card.json`
> 아티팩트 하나를 올립니다. MLflow **metric으로는 기록하지 않고**, run 상태도
> `RUNNING`으로 남습니다. 그래프 비교를 기대하면 어긋납니다. 숫자는 카드 JSON
> 안에 있으니, 데모에서 그래프가 필요하면 별도로 metric을 기록하세요.

**UI는 대시보드에 임베드되어 있습니다.** 별도 Route가 없습니다. 대시보드의
module federation 설정(`federation-config`)에 `mlflow`와 `mlflowEmbedded`가 등록되어
있고, `https://rh-ai.apps.<도메인>/mlflow`로 열립니다. `--static-prefix=/mlflow`가
그 이유입니다.

---

## 운영

| 작업 | 명령 |
|---|---|
| 정지 | `./scripts/10-stop-cluster.sh` |
| 재시작 | `./scripts/11-start-cluster.sh` |
| GPU만 내리기 | `oc -n openshift-machine-api scale machineset <NAME> --replicas=0` |
| 완전 삭제 | `./scripts/99-destroy-cluster.sh` |

**정지 시 주의 두 가지** (실제로 겪은 것)

**자동 기동이 GPU 노드를 빠뜨릴 수 있습니다.** 다른 노드는 켜졌는데 GPU만 `stopped`로
남았고, 그 결과 `machine-config`가 Degraded였습니다. 환경이 자동으로 올라온 뒤에는
GPU를 따로 확인하세요.

**정지된 인스턴스의 재기동도 용량 경쟁을 합니다.** `InsufficientInstanceCapacity`로
10번 연속 거부되다 11번째에 잡힌 적이 있습니다. "내 인스턴스니까 켜면 된다"가 아닙니다.

**장기 정지 후 복구** — 24시간을 넘기면 노드 인증서가 로테이션됩니다. 6일 정지도
20분 만에 복구됐지만, CSR을 **모든 노드가 Ready가 될 때까지 반복 승인**해야 합니다.
고정 횟수로는 부족합니다. 중간에 전 노드가 NotReady로 떨어지는 것(`NetworkPluginNotReady`)과
`oc get route`가 실패하는 것은 정상 경과입니다.

---

전체 개요는 [README](README.md), 각 단계의 배경과 진단 과정은 번호 문서를 보세요.
