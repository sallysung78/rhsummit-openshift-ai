# 빈 AWS 계정에서 데모 가능한 OpenShift AI까지 — 전체 순서

한 번에 따라갈 수 있는 체크리스트입니다. 각 단계의 배경과 겪은 함정은 번호가 붙은
개별 문서에 있고, 여기서는 **무엇을 어떤 순서로 하고 무엇으로 확인하는지**만 다룹니다.

전부 처음부터 하면 **약 3시간**이고 대부분은 기다리는 시간입니다.

```
0  사전 준비         30분   계정·쿼터·pull secret 확인
1  OpenShift 설치    45분   ← 대기가 긴 구간
2  OpenShift AI      25분
3  GPU 노드          30분   ← 용량 확보가 변수
4  GPU 관측          10분
5  오브젝트 스토리지   15분
6  모델 S3 적재       20분   ← 클러스터 안에서 받아야 함
7  프로젝트와 연결     5분
8  파이프라인 서버    10분
9  모델 등록·배포     20분
```

범례: ✅ 완료 · ⬜ 예정

---

## 0. 사전 준비 ✅

빈 AWS 계정이라도 IPI 설치는 네 가지를 전제합니다.

| 항목 | 확인 |
|---|---|
| Route 53 **퍼블릭** 호스티드 존 | `aws route53 list-hosted-zones` |
| Red Hat pull secret | [console.redhat.com](https://console.redhat.com/openshift/install/pull-secret) |
| IAM 자격증명 | `aws sts get-caller-identity` |
| 쿼터 | Standard vCPU ≥ 36, **Elastic IP ≥ 3** |

Elastic IP가 조용한 병목입니다. AZ마다 NAT 게이트웨이가 하나씩 생기고 각각 EIP를
먹는데 기본 한도가 5입니다.

**리전 선택이 나중의 GPU를 결정합니다.** 클러스터 생성 후에는 바꿀 수 없고 GPU 노드는
같은 VPC에 있어야 합니다. 쓰려는 GPU 타입이 3개 AZ 모두에 제공되는 리전을 고르세요.

→ 자세히: [1단계 문서](01-openshift-설치.md)

## 1. OpenShift 설치 ✅ — 45분

```bash
source scripts/00-env.sh
./scripts/01-create-cluster.sh
```

42분 걸립니다. 그 전에 30초로 설정 오류를 잡을 수 있습니다:

```bash
openshift-install create manifests --dir <디렉터리>   # 리소스 생성 없이 검증만
```

**확인**

```bash
oc get clusterversion                      # Available=True
oc get nodes                               # 6대 Ready
oc get co | grep -v 'True.*False.*False'   # 비어 있으면 정상
```

⚠️ `cluster/` 디렉터리를 지우지 마세요. 나중에 삭제할 때 필요합니다.

## 2. OpenShift AI 설치 ✅ — 25분

**순서가 중요합니다. RWX 스토리지가 먼저입니다.**

```bash
./scripts/03-efs-setup.sh        # EFS + 마운트 타깃 + StorageClass
./scripts/04-verify-catalog.sh   # ★ 적용 전에 채널 대조
./scripts/05-install-rhoai.sh
```

**`04-verify-catalog.sh`를 건너뛰지 마세요.** RHOAI의 `stable` 채널은 아직 2.x를
가리킵니다. 3.x를 쓰려면 `stable-3.5`처럼 명시해야 하고, 2.x와 3.x는 API가 달라
매니페스트가 그대로 적용되지 않습니다.

**확인**

```bash
oc get datasciencecluster default-dsc -o jsonpath='{.status.phase}'   # Ready
oc -n redhat-ods-applications get pods                                 # 21개 내외
oc -n openshift-ingress get route data-science-gateway                 # rh-ai.apps...
```

→ 자세히: [2단계 문서](02-openshift-ai-설치.md)

## 3. GPU 노드 연결 ✅ — 30분

```bash
./scripts/06-gpu-find-capacity.sh g6e.2xlarge:<AZ-a> g6e.2xlarge:<AZ-b> g6e.2xlarge:<AZ-c>
oc apply -f manifests/05-machineset-gpu.yaml
./scripts/07-gpu-operators.sh    # NFD + NVIDIA GPU Operator
```

**용량이 가장 큰 변수입니다.** H200·H100은 온디맨드 용량이 없을 수 있고, AWS가
알려주는 "다른 AZ를 쓰라"는 안내는 신뢰할 수 없습니다 — 실제로 기동해 봐야 압니다.
탐색 스크립트는 실패한 시도를 정리하므로 재시도 비용이 없습니다.

**확인**

```bash
oc get nodes -l node-role.kubernetes.io/gpu \
  -o custom-columns='NODE:.metadata.name,GPU:.status.allocatable.nvidia\.com/gpu'
oc get clusterpolicy    # ready
```

> `allocatable`이 **`0`이면 준비 안 된 것**입니다. 값이 있는지가 아니라 **0보다 큰지**를
> 봐야 합니다. 드라이버 재빌드에 5~10분 걸립니다.

→ 자세히: [3단계 문서](03-gpu-노드-연결.md)

## 4. GPU 관측 ✅ — 10분

```bash
./scripts/09-gpu-observability.sh
```

대시보드(Observe → Dashboards → NVIDIA GPU / DCGM), 콘솔 링크, 비용·건강 알림 6종을
추가합니다.

**GPU 메트릭에는 user workload monitoring이 필요 없습니다.** GPU Operator가 자기
네임스페이스에 `openshift.io/cluster-monitoring=true`를 붙이기 때문입니다.

⚠️ 플랫폼 메트릭을 질의하는 알림은 **반드시 플랫폼 모니터링 네임스페이스**에 두세요.
사용자 네임스페이스에 두면 UWM이 셀렉터에 `namespace="..."`를 주입해 **조용히 발동하지
않습니다.** 오류도 안 납니다.

## 5. 오브젝트 스토리지 ✅ — 15분

모델과 파이프라인 아티팩트를 둘 곳이 필요합니다.

```bash
aws s3api create-bucket --bucket <INFRA_ID>-models --region <REGION> \
  --create-bucket-configuration LocationConstraint=<REGION>
aws s3api put-public-access-block --bucket <INFRA_ID>-models \
  --public-access-block-configuration BlockPublicAcls=true,IgnorePublicAcls=true,BlockPublicPolicy=true,RestrictPublicBuckets=true
aws s3api put-bucket-encryption --bucket <INFRA_ID>-models \
  --server-side-encryption-configuration '{"Rules":[{"ApplyServerSideEncryptionByDefault":{"SSEAlgorithm":"AES256"}}]}'
```

프리픽스는 `models/`와 `pipelines/` 둘로 나눕니다.

⚠️ **OpenShift 내부 레지스트리 버킷(`<INFRA_ID>-image-registry-...`)을 재사용하지
마세요.** 레지스트리 오퍼레이터 소유라 모델 아티팩트가 정리될 수 있습니다.

**admin 키를 쓰지 말고 전용 IAM 사용자를 만드세요.** 이 버킷에만 권한을 주고, 다른
버킷·IAM·EC2에서 실제로 거부되는지 확인하세요.

## 6. 모델을 S3에 적재 ✅ — 20분

**노트북이 아니라 클러스터 안에서 받으세요.** 18 GiB를 가정용 회선으로 내려받는 건
방향이 틀렸습니다. 클러스터에서 돌리면 HF→S3 양쪽 다 AWS 네트워크입니다.

```yaml
resources:
  # emptyDir 은 ephemeral-storage 한도에 산입된다.
  # 가중치보다 넉넉히 잡지 않으면 파드가 Evicted 된다.
  requests: { ephemeral-storage: 30Gi }
  limits:   { ephemeral-storage: 50Gi }
```

이 한도를 4Gi로 뒀다가 18 GiB 모델에서 세 번 연속 퇴거당했습니다.

**확인**

```bash
aws s3 ls --summarize --recursive s3://<INFRA_ID>-models/models/
```

## 7. 프로젝트와 연결 ✅ — 5분

RHOAI 대시보드에서 Data Science Project를 만들고 S3 연결을 추가합니다.
연결 유형은 **`S3 compatible object storage - v1`** 입니다.

시크릿을 직접 만들어도 됩니다 — 대시보드가 인식하려면 라벨과 어노테이션이 필요합니다:

```yaml
labels:
  opendatahub.io/dashboard: "true"
  opendatahub.io/managed: "true"
annotations:
  opendatahub.io/connection-type-ref: s3
data 키: AWS_ACCESS_KEY_ID / AWS_SECRET_ACCESS_KEY / AWS_S3_ENDPOINT
        / AWS_DEFAULT_REGION / AWS_S3_BUCKET
```

**촬영이 있다면 연결을 미리 만들어 두세요.** 카메라 앞에서 시크릿 키를 타이핑하지
않게 됩니다.

## 8. 파이프라인 서버 ✅ — 10분

파이프라인은 **프로젝트마다 자기 서버**가 있어야 합니다. 컴포넌트가 Managed인 것과
별개입니다.

`spec`에서 **필수는 `objectStorage` 하나**입니다. Argo가 스텝 사이 아티팩트를 파드
파일시스템이 아니라 오브젝트 스토리지로 넘기기 때문입니다.

```bash
oc apply -f manifests/70-pipeline-server.yaml
```

5단계 버킷을 `basePath: pipelines`로 재사용하고, 자격증명은 7단계 연결을 그대로
참조합니다. DB는 지정하지 않으면 네임스페이스 안에 MariaDB가 생성됩니다.

**확인** — 파드가 떴다는 것만으로는 부족합니다. operator 자체 검증을 보세요:

```bash
oc -n <PROJECT> get dspa dspa -o json | jq -r '.status.conditions[]|"\(.type)=\(.status)"'
#   DatabaseAvailable=True
#   ObjectStoreAvailable=True     ← S3 연결이 실제로 검증된 것
#   Ready=True
```

`WebhookReady=False`와 `ManagedPipelineValid=False`는 정상입니다 — 각각 S3 저장소를
쓸 때 해당 없음, 샘플 파이프라인을 끈 결과입니다.

## 9. 모델 등록과 배포 ⬜ — 20분

Model Registry에 등록하고 KServe로 배포합니다.

| 고를 값 | |
|---|---|
| 서빙 런타임 | `vLLM NVIDIA GPU ServingRuntime for KServe` |
| 모델 포맷 | `vLLM` |
| 하드웨어 프로파일 | GPU 프로파일 (예: `NVIDIA L40S (1 x 48GB)`) |

⚠️ **하드웨어 프로파일은 선택 사항이 아닙니다.** GPU 노드에 테인트가 걸려 있고
대응하는 톨러레이션을 이 프로파일이 들고 있습니다. 잘못 고르면 파드가 `Pending`에
머뭅니다.

첫 vLLM 기동은 **5~10분**(이미지 풀 + 가중치 로드), 이후는 2~3분입니다.

**확인**

```bash
oc -n <PROJECT> get inferenceservice
curl -sk https://<endpoint>/v1/chat/completions -H 'Content-Type: application/json' \
  -d '{"model":"<모델명>","messages":[{"role":"user","content":"안녕"}]}'
```

→ 자세히: [4단계 문서](04-모델-배포-시나리오.md)

## 10. 검증 파이프라인 ⬜ (기획 단계)

**설계를 지배하는 제약은 GPU 개수입니다.** 1장이면 두 버전을 동시에 띄울 수 없어
파이프라인이 직렬이어야 합니다:

```
배포 → 검증 → 내리기 → (다음 버전) 배포 → 검증 → 내리기
```

1회 실행에 15~25분이라 **실시간 촬영에는 맞지 않습니다.** 미리 돌려둔 결과를 보여주고
실행 버튼만 촬영하는 편집이 낫습니다.

파이프라인 구성 제안:

```
preflight → deploy → smoke → evaluate → measure → gate → promote → teardown
```

`teardown`을 반드시 넣으세요. 파이프라인이 GPU를 잡고 있으면 수동 데모가 막힙니다.

## 11. EvalHub 기반 평가 ⬜ (미적용)

**RHOAI 3.5에는 `EvalHub`가 GA로 들어 있습니다.** lm-evaluation-harness, GuideLLM,
RAGAS, LightEval을 REST API로 묶어주는 서비스이고 TrustyAI 오퍼레이터가 관리합니다.

평가를 직접 구현하기 전에 이것부터 보세요. 특히 **GuideLLM**은 TTFT, inter-token
latency, throughput을 측정해 주는데, 버전 교체 데모에서 "무엇을 얻고 무엇을 잃었는지"를
숫자로 보여주는 데 적합합니다.

```yaml
apiVersion: trustyai.opendatahub.io/v1alpha1
kind: EvalHub
spec:
  replicas: 1
  env:
    - name: MLFLOW_TRACKING_URI
      value: "http://mlflow.<namespace>.svc.cluster.local:5000"
```

**필요한 것**: DSC에서 `trustyai`와 `mlflowoperator`를 `Managed`로 전환, HuggingFace
토큰, 워커 자원 여유.

⚠️ 대시보드의 `disableLMEval: false` 플래그만 켜면 **메뉴는 보이지만 동작하지
않습니다.** 백엔드인 `trustyai`가 함께 있어야 합니다.

> 정확도 벤치마크 전량 실행은 GPU 1장으로는 비현실적입니다(KMMLU만 35,030문항).
> 샘플을 제한하거나, 성능 측정 위주로 쓰세요. 모델 관리가 주제라면 정확도 점수보다
> 성능 비교가 메시지에 맞습니다.

---

## 운영

| 작업 | 명령 |
|---|---|
| 정지 | `./scripts/10-stop-cluster.sh` |
| 재시작 | `./scripts/11-start-cluster.sh` |
| GPU만 내리기 | `oc -n openshift-machine-api scale machineset <NAME> --replicas=0` |
| 완전 삭제 | `./scripts/99-destroy-cluster.sh` |

**정지 시 주의 두 가지** (실제로 겪은 것)

**자동 기동이 GPU 노드를 빠뜨릴 수 있습니다.** 다른 노드는 켜졌는데 GPU만 `stopped`로
남았고, 그 결과 `machine-config`가 Degraded였습니다. 환경이 자동으로 올라온 뒤에는
GPU를 따로 확인하세요.

**정지된 인스턴스의 재기동도 용량 경쟁을 합니다.** `InsufficientInstanceCapacity`로
10번 연속 거부되다 11번째에 잡힌 적이 있습니다. "내 인스턴스니까 켜면 된다"가 아닙니다.

**장기 정지 후 복구** — 24시간을 넘기면 노드 인증서가 로테이션됩니다. 6일 정지도
20분 만에 복구됐지만, CSR을 **모든 노드가 Ready가 될 때까지 반복 승인**해야 합니다.
고정 횟수로는 부족합니다. 중간에 전 노드가 NotReady로 떨어지는 것(`NetworkPluginNotReady`)과
`oc get route`가 실패하는 것은 정상 경과입니다.

---

전체 개요는 [README](README.md), 각 단계의 배경과 진단 과정은 번호 문서를 보세요.
