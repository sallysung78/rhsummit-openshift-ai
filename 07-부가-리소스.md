# OpenShift AI 외에 필요했던 리소스

"OpenShift AI 설치"만으로 끝나지 않습니다. 컴포넌트를 켜는 순간 **DB·스토리지·오퍼레이터·
IAM 사용자가 따라 생깁니다.** 어느 것이 자동으로 생기고 어느 것을 내가 준비해야 하는지가
헷갈리기 쉬워, 이 환경을 실제로 조회해서 정리했습니다.

아래는 전부 **실행 중인 클러스터에서 읽은 값**입니다.

---

## 1. 데이터베이스 — 5개가 돕니다 (MaaS 포함)

| DB | 위치 | 쓰는 곳 | 영속성 | 누가 만드나 |
|---|---|---|---|---|
| PostgreSQL 16 | `rhoai-model-registries` / `model-catalog-postgres` | **모델 카탈로그** | ⚠️ `emptyDir` — **영속 안 됨** | RHOAI 자동 |
| PostgreSQL 16 | `rhoai-model-registries` / `<이름>-registry-postgres` | **모델 레지스트리 인스턴스** | PVC 1개 (RWO) | 레지스트리 생성 시 |
| MariaDB 10.5 | `<PROJECT>` / `mariadb-dspa` | **파이프라인 메타데이터** | PVC 1개 (RWO) | DSPA 생성 시 |
| SQLite | 파드 내부 | **EvalHub**, **MLflow** | MLflow는 PVC, EvalHub는 파드 수명 | CR의 `spec` 기본값 |
| PostgreSQL 16 | `redhat-ai-gateway-infra` / `maas-postgres` | **MaaS API** (키·구독·사용량) | PVC 5 Gi | **내가 만들어야 함** — 오퍼레이터가 안 만듦 |

### 알아둘 것 네 가지

**모델 레지스트리는 인스턴스마다 PostgreSQL이 하나씩 생깁니다.** 컴포넌트를 켜는 것과
인스턴스를 만드는 것은 다른 일이고, 인스턴스를 만들 때 DB가 따라옵니다. 레지스트리를
팀별로 여러 개 만들면 Postgres도 그만큼 늘어납니다.

**모델 카탈로그 DB는 `emptyDir`입니다.** 파드가 재시작하면 내용이 사라집니다. 카탈로그는
외부 검증 목록을 읽어 오는 **읽기 전용** 성격이라 다시 채워지지만, "DB가 있으니 영속되겠지"
라고 가정하면 안 됩니다.

```bash
oc -n rhoai-model-registries get pod -o jsonpath='{.items[*].spec.volumes[*].emptyDir}'
```

**파이프라인 DB는 MariaDB이고 charset이 latin1입니다.** 파이프라인 스펙이 여기 저장되므로
**컴포넌트 코드에 한국어를 쓸 수 없습니다.** → [11단계](00-전체-구축-순서.md)

**EvalHub와 MLflow의 SQLite는 데모용입니다.** CR의 `spec.database.type`과
`spec.backendStoreUri`로 지정한 값이고, 여러 사용자가 동시에 쓰는 환경이라면 PostgreSQL로
바꿔야 합니다. 이 데모에서는 단일 사용자라 SQLite로 두었습니다.

---

## 2. 스토리지

### 클러스터 내부 (PVC)

| PVC | 네임스페이스 | 모드 | 용량 | 용도 |
|---|---|---|---|---|
| `mariadb-dspa` | `<PROJECT>` | RWO | — | 파이프라인 메타데이터 |
| `<이름>-registry-postgres-storage` | `rhoai-model-registries` | RWO | — | 레지스트리 DB |
| `mlflow-pvc` | `redhat-ods-applications` | RWO | 10 Gi | MLflow SQLite + 로컬 파일 |

전부 `gp3-csi`(기본 StorageClass, EBS)입니다. **세 개 다 RWO**입니다.

### RWX(EFS)는 준비했지만 이 구성에서는 쓰이지 않았습니다

| | |
|---|---|
| StorageClass | `efs-sc` (`efs.csi.aws.com`) |
| 파일시스템 | 1개, 마운트 타깃 3개, 사용량 **6 KiB** |

> **솔직하게**: RWX가 필요할 것으로 보고 EFS를 먼저 깔았는데, **실제로 이 구성에서는
> 아무것도 쓰지 않았습니다.** 워크벤치에서 여러 파드가 같은 볼륨을 공유하거나 분산 학습을
> 할 때 필요합니다. 모델 서빙과 파이프라인만 한다면 **EFS 없이도 됩니다.**
> 비용을 줄이려면 이 단계를 건너뛰어도 좋습니다.

### 오브젝트 스토리지 (S3)

버킷 하나에 prefix로 나눕니다.

| prefix | 용도 | 누가 씁니다 |
|---|---|---|
| `models/` | 모델 가중치 | KServe storage-initializer |
| `pipelines/` | 파이프라인 실행 산출물·로그 | DSPA |
| `mlflow/` | MLflow 아티팩트 (평가 카드 등) | MLflow |

⚠️ **OpenShift 내부 레지스트리 버킷(`<INFRA_ID>-image-registry-...`)을 재사용하지 마세요.**
클러스터가 관리하는 버킷이라 라이프사이클이 얽힙니다. 별도 버킷을 만드세요.

---

## 3. 오퍼레이터 — RHOAI 말고 3개 더 (MaaS를 켜면 +4)

MaaS를 켜면 **Red Hat Connectivity Link**(`rhcl-operator`)가 필요하고, 이것이 authorino ·
limitador · dns 오퍼레이터를 함께 끌어옵니다 → [11-maas-데모.md](11-maas-데모.md)


| 오퍼레이터 | 카탈로그 | 왜 필요한가 |
|---|---|---|
| **NFD** (Node Feature Discovery) | redhat-operators | 노드의 GPU를 라벨로 노출. GPU Operator의 전제 |
| **NVIDIA GPU Operator** | **certified-operators** | 드라이버·런타임·DCGM. `v26.3` 채널 |
| **AWS EFS CSI Driver** | redhat-operators | RWX 스토리지 (위 단서 참고) |

**GPU Operator는 `certified-operators` 카탈로그에 있습니다.** `redhat-operators`에서 찾으면
나오지 않습니다.

⚠️ **OperatorGroup이 없는 네임스페이스에 Subscription을 만들면 조용히 멈춥니다.**
InstallPlan이 생기지 않고 status가 비어서, 설정 오류가 다운로드 지연처럼 보입니다.
`openshift-cluster-csi-drivers`가 그런 경우였습니다. → [2단계](02-openshift-ai-설치.md)

---

## 4. AWS 리소스

### 컴퓨트

| 역할 | 타입 | 수 |
|---|---|---|
| 마스터 | `m6i.xlarge` | 3 |
| 워커 | `m6i.2xlarge` | 3 |
| **GPU 워커** | `g6e.2xlarge` (L40S 48 GB) | 1 |

GPU 노드는 **별도 MachineSet**으로 추가합니다. 설치 시 만들어지지 않습니다.

### 그 외

| 리소스 | 수 | 비고 |
|---|---|---|
| S3 버킷 | 2 | 모델용 1 + 클러스터 내부 레지스트리용 1(자동 생성) |
| EFS 파일시스템 | 1 | 마운트 타깃 3 (AZ마다 하나) |
| IAM 사용자 | 9 | **8개는 설치 시 자동 생성**, 1개만 내가 만든 것 |

**IAM 사용자 8개는 `openshift-install`이 만듭니다.** EBS CSI, EFS CSI, 이미지 레지스트리,
인그레스, machine-api, 클라우드 네트워크 컨트롤러 등이 각자 쓸 자격증명을 갖습니다.
클러스터를 지울 때 함께 정리되므로 손대지 마세요.

**내가 만든 것은 S3 전용 사용자 하나뿐입니다.**

> **admin 키를 모델 접근에 쓰지 마세요.** 버킷 하나에만 권한이 있는 전용 IAM 사용자를
> 만드세요. 이 자격증명은 클러스터 안의 시크릿에 저장되고 여러 컴포넌트가 참조합니다.
> admin 키를 넣으면 그 시크릿을 읽을 수 있는 사람이 계정 전체를 만지게 됩니다.

VPC·서브넷·NAT·Route 53 레코드·보안 그룹은 IPI 설치가 전부 만들고 삭제 시 정리합니다.

---

## 5. 시크릿과 권한 — 놓치기 쉬운 것

| 무엇 | 어디에 | 왜 |
|---|---|---|
| S3 연결 시크릿 | `<PROJECT>` | 모델 적재·서빙 |
| **같은 시크릿 복사본** | **`redhat-ods-applications`** | **MLflow가 여기서 찾습니다** |
| ClusterRole `nodes` 읽기 | 클러스터 | 파이프라인 `preflight` 단계 |
| EvalHub 접근 RoleBinding | `<PROJECT>` | 파이프라인 `measure` 단계 |

**MLflow CRD는 클러스터 범위라 `redhat-ods-applications`에 배포됩니다.** 그래서
`spec.envFrom`이 참조하는 S3 시크릿도 **그 네임스페이스에** 있어야 합니다. 모델 프로젝트에만
만들어 두면 마이그레이션 Job이 `secret not found`로 죽는데, 그 이벤트는 내가 보는
네임스페이스에 나타나지 않습니다. → [13단계](00-전체-구축-순서.md)

**연결 시크릿에는 어노테이션이 두 개 필요합니다.**
`opendatahub.io/connection-type-ref: s3`만 있고 `opendatahub.io/connection-type: s3`가 없으면
컨트롤러가 `storage-config`를 만들 때 `type`을 비워 두고, 웹훅이
`storage type [] is not supported`로 배포를 거부합니다.

---

## 6. 요약 — 내가 준비할 것 vs 따라오는 것

**내가 준비해야 하는 것**

- S3 버킷 1개 + 전용 IAM 사용자 1명
- GPU MachineSet
- 오퍼레이터 3개 (NFD, GPU, EFS)
- 연결 시크릿과 그 복사본, 추가 RBAC 2개

**켜면 따라오는 것**

- PostgreSQL 2개 (카탈로그, 레지스트리 인스턴스마다)
- MariaDB 1개 + MLMD gRPC 서버 (파이프라인)
- PVC 3개, IAM 사용자 8명, 인증서·서비스·라우트 일체

**필요 없을 수도 있는 것**

- EFS — 이 구성에서는 6 KiB만 쓰고 놀았습니다. 워크벤치 공유 볼륨이나 분산 학습을 하지
  않는다면 건너뛰어도 됩니다.
