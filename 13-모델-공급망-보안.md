# 모델 공급망 보안 — 어디서 받아, 무엇을 실행하는가

오픈소스 모델을 받는다는 것은 **남이 만든 수십 GB 바이너리와, 경우에 따라 파이썬 코드를
GPU 서버에서 실행한다**는 뜻입니다. 이 문서는 커뮤니티 원본과 Red Hat 검증 모델이 이
지점에서 어떻게 다른지, **이 클러스터에서 직접 확인한 것만** 정리합니다.

## 비교 — 같은 모델, 두 경로

`Qwen/Qwen3.6-35B-A3B`(커뮤니티 원본)와 `RedHatAI/Qwen3.6-35B-A3B-FP8`(Red Hat 검증).

| | 커뮤니티 원본 | Red Hat 검증 |
|---|---|---|
| **받는 곳** | huggingface.co 저장소 | **registry.redhat.io** OCI modelcar 이미지 (`modelcar-redhatai-qwen3-6-35b-a3b-fp8:3.0`) |
| **버전 고정** | 저장소 브랜치 — 바뀔 수 있음 | 컨테이너 이미지 태그·digest |
| **보안 레드팀 평가** | 카탈로그에 **데이터 없음** | **garak 프로브 6종** 결과 게시 (프롬프트 인젝션 · 시스템 프롬프트 우회 · 탈옥) |
| **검증된 스택** | 미상 | RHOAI 3.5 · RHAIIS 3.5 · vLLM 0.24 |
| **정확도·성능 근거** | 모델 제작사 발표 | 원본 대비 회복률 + A100/H100/H200 성능 측정 |
| **가중치 형식** | safetensors | safetensors |

카탈로그 전체로 보면: **validated 모델 89개 전부**가 registry.redhat.io modelcar로 배포되고,
보안 평가 데이터가 있는 모델은 **122개 중 41개**입니다.

## 확인한 위험 하나 — `--trust-remote-code`

이 클러스터의 vLLM 서빙 런타임(`vllm-cuda-runtime`)은 **기본 인자로 `--trust-remote-code`를
켭니다.** 모델 저장소에 `.py` 파일이 있으면 그 코드가 GPU 서버에서 그대로 실행된다는 뜻입니다.

| 저장소 | `.py` | pickle 계열(`.bin` `.pt` `.pkl`) |
|---|---|---|
| `Qwen/Qwen3.6-35B-A3B` | 없음 | 없음 |
| `RedHatAI/Qwen3.6-35B-A3B-FP8` | 없음 | 없음 |

**이번 두 모델은 둘 다 안전한 형식입니다.** 과장하지 마세요. 요점은 "이 커뮤니티 모델이
위험했다"가 아니라 **"런타임이 원격 코드를 기본 허용하므로, 무엇을 받느냐가 곧 무엇을
실행하느냐다"** 입니다. 커뮤니티 모델은 받을 때마다 이걸 확인해야 하고, 검증 모델은 Red Hat이
빌드·검증한 산출물이라 그 확인이 줄어듭니다.

> 데모 멘트: *"이 런타임은 원격 코드를 기본으로 허용합니다. 그래서 모델을 어디서 받느냐가
> 보안 문제입니다. 검증 모델은 Red Hat 레지스트리의 컨테이너 이미지로 오고, 레드팀 평가
> 결과가 붙어 있습니다."*

## 검증 모델이 덜어 주는 우려, 그래도 남는 것

**덜어 주는 것**

- **출처** — Red Hat 레지스트리에서 받은 컨테이너 이미지. 임의 저장소가 아닙니다.
- **레드팀 평가** — 프롬프트 인젝션·탈옥 프로브를 이미 돌렸고 항목이 공개돼 있습니다.
- **스택 호환** — 어떤 RHOAI·vLLM 버전에서 검증됐는지 명시됩니다.

**그래도 남는 것 (플랫폼팀 몫)**

- 우리 가속기에서의 동작 — L40S는 검증 목록에 없었습니다 ([12](12-검증-범위.md)의 2단계)
- 런타임 설정 — `--trust-remote-code`를 쓸지는 우리가 정합니다
- 서비스별 가드레일 — 모델 수준 안전성 ≠ 서비스 수준 정책 ([12](12-검증-범위.md)의 9단계)

## 인용 시 주의 — 과장하면 신뢰를 잃습니다

- **garak 결과 수치는 인용하지 마세요.** 현재 카탈로그 데이터는 제공자가 `dev-garak-kfp`로
  표시된 예비값이고, 공격 성공률 필드와 `pass`·`threshold`·`lower_is_better` 필드가 서로
  맞지 않습니다. 말할 것은 **"어떤 항목으로 평가했는지가 공개돼 있다"** 까지입니다.
- **이미지 서명은 이 클러스터에서 직접 검증하지 않았습니다.** "Red Hat 레지스트리에서 온다"는
  확인했지만 "서명이 검증됐다"고 말하려면 리허설 전에 서명 검증을 한 번 해 두세요.
- **"검증 모델은 안전하다"는 말은 하지 마세요.** "우려를 덜어 준다"와 "보증한다"는 다릅니다.
